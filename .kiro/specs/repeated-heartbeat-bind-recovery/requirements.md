# Requirements

1. Bind a promoted repeated-recovery successor without a provider pull-request-base assertion.
2. Adopt only the exact transition-counter-plus-one active claim after bind response loss.
3. Preserve the sealed plan, promoted claim, target scope, reviewed head, and review request identity.
4. Reject every other cloud state without changing local lease, task binding, source, refs, or PR state.
