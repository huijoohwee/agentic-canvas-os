# Design

The trusted pull-request event path captures a canonical descendant proof from
fetched Git refs. Provider mapping accepts the historical claim base only for
reviewed verification with exact claim, review, head, and proof identities.
