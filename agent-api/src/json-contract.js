// Compatibility for existing Canvas JSON callers; implementation and bounds live in pinned OS.
export { canonicalizeJson, freezeJson, normalizeJson, serializedJsonLength } from "agentic-os/context/json";
