// Compatibility path for existing callers; implementation is owned by agentic-os.
export { SANDBOX_AGENT_CAPABILITIES, SandboxAgentBlock, assertExactKeys, assertIdentifier, assertPositiveInteger, capabilityForOperation, normalizeAttestation, normalizeAuthorization, normalizeCapabilities, normalizeCloseOutcome, normalizeContainmentProof, normalizeContainmentVerifier, normalizeCost, normalizeExecutionOutcome, normalizeOpenRequest, normalizeOperation, normalizeProviderDescriptor, normalizePublicOutput, normalizeSandboxAdapter, normalizeSessionOutcome, normalizeSnapshotOutcome, normalizeSuspendOutcome, normalizeWorkspace, requireSandboxMethod } from "agentic-os/agents/sandbox-agent-contract";
