---
title: "Knowgrph Agentic Canvas OS Runtime Proof"
graphId: "md:knowgrph-agentic-canvas-os-runtime-proof"
doc_type: "Runtime Proof Ledger"
date: "2026-07-31"
lang: "en-US"
schema: "agentic-canvas-os-runtime-proof/v1"
frontmatter_contract: "required"
status: "runtime-ready"
runtime_scope: "Agentic Canvas OS docs control surface"
runtime_proof: "RUNTIME-PROOF.md"
source_docs:
  - "AGENT-SWARM.md"
  - "AGENT-TOOLKIT.md"
  - "AGENT-DEFINITIONS.md"
  - "MODELS-AND-PROVIDERS.md"
  - "LIVE-AGENT-PROVIDER-PROOF.md"
  - "SOUL.md"
  - "USER.md"
  - "FACTS.md"
  - "AGENTS.md"
  - "AGENTIC-SDLC-RUNTIME.md"
  - "MANAGED-IMPLEMENTATION-RUNS.md"
  - "DICTIONARY-COMMAND.md"
  - "DICTIONARY-SEMANTIC.md"
  - "DICTIONARY-BINDING.md"
  - "HARNESS-CONTRACTS.md"
  - "INSTRUCTION-AUDIT.md"
  - "INSTRUCTION-QUALITY-EVALUATION.md"
  - "MCP-GATEWAY.md"
  - "MEMORY.md"
  - "PRD-TAD.md"
  - "PROMPT-PRESETS.md"
  - "README.md"
  - "RUNTIME-PROOF.md"
  - "RUNTIME-READINESS.md"
  - "SKILLS.md"
  - "kanban.md"
  - "VALIDATION-RUNBOOK.md"
publish_policy: "protected green main authorizes Dev integration only; forward Production requires exact-candidate human authorization"
external_runtime_policy: "Knowgrph, Prod mirror, and Cloudflare require separate exact-candidate proof from the human-authorized release controller"
kgCanvasSurfaceMode: "2d"
kgCanvasRenderMode: "2d"
kgCanvas2dRenderer: "storyboard"
kgDocumentSemanticMode: "document"
kgFrontmatterModeEnabled: true
kgMultiDimTableModeEnabled: true
kgDocumentStructureBaselineLock: false
socket_types:
  proof_parse_signal:
    label: "Proof parse signal"
    cardinality: "one-to-many"
  proof_route_signal:
    label: "Proof route signal"
    cardinality: "one-to-many"
  proof_status_signal:
    label: "Proof status signal"
    cardinality: "one-to-one"
flow:
  direction: {key: direction, type: string, value: "LR"}
  edgeType: {key: edgeType, type: string, value: "smoothstep"}
  balancedViewportPreset: {key: balancedViewportPreset, type: string, value: "widgetFrontmatter"}
  computed: {key: computed, type: boolean, value: true}
  snapToGrid: {key: snapToGrid, type: boolean, value: true}
  nodes:
    - id: {key: id, type: string, value: "parse_evidence"}
      type: {key: type, type: string, value: "source"}
      label: {key: label, type: string, value: "Parse evidence"}
      lane: {key: lane, type: string, value: "parse"}
      position: {key: position, type: object, value: {x: 0, y: 0}}
      handles: {key: handles, type: list, value: ["parse.out"]}
      "flow:portTypes": {key: "flow:portTypes", type: list, value: ["proof_parse_signal"]}
    - id: {key: id, type: string, value: "route_evidence"}
      type: {key: type, type: string, value: "process"}
      label: {key: label, type: string, value: "Route evidence"}
      lane: {key: lane, type: string, value: "route"}
      position: {key: position, type: object, value: {x: 280, y: 0}}
      handles: {key: handles, type: list, value: ["route.in", "route.out"]}
    - id: {key: id, type: string, value: "artifact_scan"}
      type: {key: type, type: string, value: "guard"}
      label: {key: label, type: string, value: "Runtime artifact scan"}
      lane: {key: lane, type: string, value: "scan"}
      position: {key: position, type: object, value: {x: 560, y: 0}}
      handles: {key: handles, type: list, value: ["scan.in", "scan.out"]}
    - id: {key: id, type: string, value: "proof_status"}
      type: {key: type, type: string, value: "observer"}
      label: {key: label, type: string, value: "Proof status"}
      lane: {key: lane, type: string, value: "proof"}
      position: {key: position, type: object, value: {x: 840, y: 0}}
      handles: {key: handles, type: list, value: ["status.in", "status.out"]}
    - id: {key: id, type: string, value: "deploy_boundary"}
      type: {key: type, type: string, value: "guard"}
      label: {key: label, type: string, value: "Deploy boundary"}
      lane: {key: lane, type: string, value: "boundary"}
      position: {key: position, type: object, value: {x: 1120, y: 0}}
      handles: {key: handles, type: list, value: ["boundary.in"]}
  edges:
    - id: {key: id, type: string, value: "parse_to_route"}
      source: {key: source, type: string, value: "parse_evidence"}
      target: {key: target, type: string, value: "route_evidence"}
      type: {key: type, type: string, value: "proof_parse_signal"}
    - id: {key: id, type: string, value: "route_to_scan"}
      source: {key: source, type: string, value: "route_evidence"}
      target: {key: target, type: string, value: "artifact_scan"}
      type: {key: type, type: string, value: "proof_route_signal"}
    - id: {key: id, type: string, value: "scan_to_status"}
      source: {key: source, type: string, value: "artifact_scan"}
      target: {key: target, type: string, value: "proof_status"}
      type: {key: type, type: string, value: "proof_status_signal"}
    - id: {key: id, type: string, value: "status_to_boundary"}
      source: {key: source, type: string, value: "proof_status"}
      target: {key: target, type: string, value: "deploy_boundary"}
      type: {key: type, type: string, value: "proof_status_signal"}
---

# Runtime Proof

The local review-ready retirement proof is source-bounded and provider-safe:
focused tests simulate lease-fenced conditional provider closure, fresh
pre-release cloud verification, lease CAS failure/replay, exact marker and
receipt joins, split source/operator sessions, invalid identity, live and dirty
owners, lifecycle classification, and admission release without calling a real
pull request. Exact operator and live provider evidence remain run-specific; see
[`LOCAL-REVIEW-RETIREMENT.md`](./LOCAL-REVIEW-RETIREMENT.md).

This ledger proves the local Agentic Canvas OS docs control surface, one
repository-owned live GitHub collaboration lifecycle, and one explicitly
approved bounded paid provider proof. It does not claim a Prod mirror write,
Cloudflare deploy, default live Worker route, or unbounded external execution.

## Proof Scope

| Scope | Included | Excluded |
|---|---|---|
| Docs parse | Every Markdown file in this folder has YAML frontmatter and parses. | Instruction-only skipped files. |
| Route consistency | `/`, `#`, `@`, skill contracts, skill variants, and computing-flow entries resolve from source docs. | FloatingPanel-only duplicate registries or aliases. |
| Facts consistency | Every `FACTS.md` `direct_resolution` token resolves into the corresponding `/`, `#`, or `@` dictionary. | Memory-only or role-only truth precedence. |
| Probe-Tree clarification contract | `PROBE-TREE.md` declares RECOMMEND, COMPARE, ASSESS, and PLAN as semantic, case-insensitive clarification topics producing 2-4 cards through the active Chat tuple; the preset and dictionaries are validated projections that preserve selected-child ownership and the runtime-recognized terminal boundary. | Query-specific hardcoding, keyword-only classification, action-verb terminal inference, stale card-local routing, zero-model fallback cards, or live provider execution. |
| Prompt preset Chat and MCP contract | Eleven source-backed presets expose one declared Chat response mode plus read-only `knowgrph.agentic_canvas_os.docs.invoke` resolution of the same registered runtime token; four semantic SME and investment variants reuse existing agent owners. | Query-specific hardcoding, stale Chat routing, unresolved MCP tokens, zero-model LLM fallbacks, duplicated agent runtimes, paid calls, or live provider proof. |
| Runtime revision identity contract | `npm run collaboration:gate` allocates run-scoped ports, external persistence, credentials, proof, and diagnostics before focused checks plus isolated owner/guest/worker proof. It requires active peers, propagation, exact revisions, one common digest, and hydration limited to two attempts; concurrent runs never reuse or stop canonical `5173`/`8787`. | Physical devices and manually assembled identity JSON are not accepted; a nonzero result blocks parity/review/integration/release but not an independently valid isolated authoring lane. |
| Cloud collaboration ledger and fencing contract | One protected remote Git ref contains a bounded append-only ledger. Claim, bind, heartbeat, review-ready, handoff, release, and verification normalize declared writes, resolve immutable actor/repository/PR identities, use GitHub server time, hash canonical state, create child commits, and update the ref with `force: false`; a same-parent race admits at most one transition, while stable idempotency replays the accepted receipt. Per-claim digest and ledger revision remain distinct, browser dispatch queues without cancellation, and local leases or PR markers are projections only. | Malformed identity, token or local-path leakage, overlap, stale claim digest or ledger ancestry, expired claims, cross-repository PRs, head/base drift, replay payload drift, CAS exhaustion, offline shared mutation, generic merge-group proof, and missing protected-check or live final-boundary verification fail closed. Contract tests alone prove no live cloud authority; runtime readiness additionally requires the protected source SHA, ledger ruleset, app-bound check, and live claim/verify/release receipts. |
| Managed implementation run contract | `/implementation.run`, `#managed-implementation-run`, `@work-item`, `@implementation-run`, `implementation.run`, and the four `knowgrph.implementation_run.*` MCP capabilities resolve from canonical sources. ACOS safely provisions and fences the task lane and emits stable machine lifecycle evidence; Knowgrph owns durable configured execution, recovery, controls, verification, and the `delivery_ready` run projection over ACOS `review_ready`. | Unknown invocation, raw shell input, unsafe path, symbolic-link escape, dirty or divergent canonical main, duplicate scope, post-claim rollback, missing runner/policy/bounds, unversioned control, PR-body loss, automatic merge by default, copied Symphony artifacts or dependency, Prod, and Cloudflare. |
| Agentic SDLC observability catalog contract | `/sdlc.observe #agentic-sdlc-observability @implementation-run @canvas @runtime-proof` resolves to `knowgrph.agentic_sdlc.observe`; one immutable local ledger receipt gates deterministic bounded GraphData and KGC projection through existing Canvas owners, with typed source, status, conformance, cache, and zero-economics evidence. | The ACOS catalog test supplies documentation-contract proof only. It does not establish current-guideline evaluator parity, protected Knowgrph integration, live Canvas rendering, cross-device parity, state promotion, Prod, or Cloudflare. |
| Soul identity contracts | `/soul.load`, `/personality.overlay`, matching `#` tags, matching `@` bindings, `soul.load`, and `personality.overlay` resolve from source docs. | Copied identity text, personality presets, prompt assembly code, hardcoded default identity strings, or live prompt runtime execution. |
| Persistent memory contracts | `/memory.write`, `/memory.compact`, `/memory.search`, `/session.search`, `/user.profile`, matching `#` tags, matching `@` bindings, and matching skill contracts resolve from source docs. | Copied memory code, database schemas, sample entries, prompt renderers, unsupported profile inference, or live memory runtime execution. |
| Append-only memory log | `MEMORY-LOG.md`, `START-WORKFLOW.md`, `RELEASE-WORKFLOW.md`, and `VALIDATION-RUNBOOK.md` require exact `## @mem-YYYYMMDDTHHmmssZ` UTC sigil blocks, complete fields, structural startup proof, and byte-prefix release proof. | Local-time or minute-only sigils, pure YAML entries, table SSOT, bolded sigils, deletion, rewrite, reorder, compaction, or insertion before EOF. |
| Context-sharded planning | `TODO.md` owns bounded routing; new tasks own immutable `todo/YYYY-MM/<context>.md` records and flat monthly shards retain immutable legacy history, with unique Context and deterministic projection proof. | Shared writable planning files, legacy mutation, duplicate Context, wrong dates, extra record changes, or embedding-first retrieval. |
| Skills system contracts | `/skill.discover`, `/skill.load`, `/skill.bundle`, `/skill.manage`, matching `#` tags, matching `@` bindings, and matching skill contracts resolve from source docs. | Copied skills, examples, layouts, prompt text, tests, fixtures, external repositories, or live skill runtime execution. |
| Instruction audit runtime | `/instruction.audit`, `#instruction-audit`, `@instruction-source`, context budgets, required intent, exact duplicate detection, and canonical-owner checks are executable locally. | Model-based style grading, automatic instruction rewrites, copied external guidance, or deploy authority. |
| Instruction task-quality evaluation | `/instruction.quality-evaluate`, `#instruction-quality`, `@instruction-eval-suite`, candidate provenance, final-answer rubrics, and human-review boundaries are executable locally. | Private-reasoning grading, hidden model execution, lexical-score overclaims, synthetic candidate promotion, or deploy authority. |
| Context files contracts | `/context.discover`, `/context.load`, `/context.audit`, `#context-file`, `#project-context`, `#cwd-discovery`, `@context-file`, `@working-directory`, `@context-policy`, and matching skill contracts resolve from source docs. | Copied context discovery code, scanner code, example files, prompt assembly text, tests, fixtures, prose, or live context runtime execution. |
| Context references contracts | `/reference.expand`, `/reference.audit`, `#context-reference`, `#inline-context`, `#attached-context`, `@file:`, `@folder:`, `@diff`, `@staged`, `@git:`, `@url:`, `@reference-policy`, `@attached-context`, and matching skill contracts resolve from source docs. | Copied context-reference parser code, prompt section text, examples, tests, fixtures, prose, or live reference runtime execution. |
| Import URL invocation contract | Exact four-token invocation `/ingest-url @url:https://example.com @reference-policy #canvas` and `url.ingest` resolve from canonical source metadata; `knowgrph.agentic_canvas_os.docs.invoke` remains read-only discovery and `knowgrph.control_local_import_url` remains the guarded browser WebMCP executor. | Extra required tokens, alias tokens, a new semantic, a copied importer, live browser execution proof, Prod mutation, or Cloudflare deployment. |
| Kanban collaboration contracts | `/kanban.task`, `/kanban.handoff`, `/kanban.sync`, `#kanban-board`, `#task-row`, `#profile-handoff`, `#worker-process`, `#multi-agent-collaboration`, `@kanban-board`, `@task-row`, `@handoff-row`, `@agent-profile`, `@worker-process`, and `kanban.collaborate` resolve from source docs. | Copied board runtimes, schema examples, hidden in-process subagent swarms, or live worker process execution. |
| Centralized planning compliance | Startup validates the index, legacy identities, and every Context record; release requires one base-absent declared record with 11 filled cells, a directive of at most 50 words, matching dates, and no legacy changes. | Repository-local todo files, shared appends, missing, duplicate, empty, overlong, misdated, extra, or destructive planning updates. |
| Tools and toolsets contracts | `/toolset.enable`, `/toolset.disable`, `#tool-function`, `#toolset`, `#platform-toolset`, `@tool-function`, `@toolset`, `@platform-surface`, and matching skill contracts resolve from source docs. | Copied tool registries, platform presets, provider examples, config snippets, tests, fixtures, prose, or live toolset mutation. |
| Tool Gateway contracts | `/tool.catalog`, `/tool.route`, `/tool.provider.select`, `/tool.gateway.audit`, matching tool-category `#` tags, matching `@` bindings, and matching skill contracts resolve from source docs. | Copied gateway code, provider tables, model lists, config examples, tests, fixtures, prose, or live tool runtime execution. |
| Tool Search runtime | Route contracts resolve from source docs; session registration, metadata-only initial exposure, exact definition loading, authorization, bounds, costs, and cleanup are executable locally. | Copied provider code, search implementation, prompts, schemas, examples, tests, fixtures, prose, fabricated context reduction, or live provider execution. |
| Programmatic tool-calling runtime | Hosted-attestation gating, caller lineage, read-only idempotent tool eligibility, bounded continuation, cost aggregation, compact final evidence, and fail-closed readiness are executable locally. | Copied provider code or examples, local JavaScript evaluation, high-impact programmatic calls, fabricated hosted proof, or live provider execution. |
| Running agents runtime | Agent-loop transitions, exclusive continuation, same-loop streaming, atomic durable paused-turn claims, cross-isolate resume, bounds, cost evidence, and fail-closed readiness are executable locally; one approved proof also covers previous-response continuation, final settlement, and handoff usage. | Copied provider agent code, SDK schemas, prompts, examples, event fixtures, tests, prose, fabricated adapter evidence, default provider wiring, or unbounded live execution. |
| Guardrails and human review runtime | Ordered input, output, tool-input, and tool-output checks plus signed exact-scoped review, atomic consumption, durable state, edit revalidation, and one reviewed concrete gateway path are executable locally. | Copied SDK code, schemas, examples, prompts, tests, fixtures, inferred approval, duplicate side effects, fabricated provider evidence, live reviewed mutation, or deployment. |
| Agent orchestration and handoffs runtime | Manager delegation, specialist conversation transfer, explicit conversation and final-answer ownership, handback, serialization, revision fences, authorization, bounds, redaction, and cost evidence are executable locally; one approved proof covers manager-owned delegation and specialist-owned handoff. | Copied provider orchestration code, SDK schemas, prompts, examples, tests, fixtures, prose, inferred ownership, hidden routers, fabricated adapter evidence, default provider wiring, or unbounded live execution. |
| Agent Swarm runtime | Exact-agent resolution, session-owned durable runs, goal-only DAG planning, atomic claims, isolated contexts, stable retry idempotency, receipt-owner verification, task/synthesis cancellation, horizontal recovery, cost, and base-agent-only synthesis are executable locally. | Caller roles/tasks/workflows/principals/signals, copied Kimi artifacts, external runtime dependencies, self-attested mutation receipts, fabricated overlap, default provider wiring, paid calls, or deployment. |
| Agent Toolkit runtime | Caller-declared revision digests, application authorization seams, server timing, telemetry trust labels, metadata-only spans and structured exports, percentile and bottleneck profiles, one owner-aggregate cost, stable-idempotency fenced evaluator calls, unique trusted-evidence comparison, deterministic multi-candidate quality/latency/cost recommendations, per-principal Durable Object admission, review-pending proposals, and alarm recovery/cleanup are executable locally. | Internal or external registry verification by default, remote-unverified comparison evidence, raw prompts/inputs/outputs/tool payloads/private reasoning, copied NVIDIA artifacts, nested cost double-counting, cross-cohort comparison, fabricated quality, prompt or parameter mutation, default network egress, automatic application, live improvement claims, or deployment. |
| Progressive Agents runtime | One exact source-backed agent run, a tool-bearing agent through the existing Function Calling gateway, and an explicit specialist workflow are executable locally through one facade. | Copied SDK code or API shapes, imported SDK dependency, embedded tool policy, inferred specialist routing, fabricated provider proof, or default provider execution. |
| Role-based Agent Team runtime | `/agent.team #role-based-agent-team @agent-team` and the four `knowgrph.agent_team.*` tools resolve through exact source and owner revisions. The canonical local stdio host supplies exact reference verification, control authorization, expiring review receipts, and replay-safe local model execution with durable effect receipts. | Caller-configured owners, role-derived authority, private-output disclosure, uncertain-effect replay, hard-coded or downloaded models, external orchestration dependency, remote Worker parity, provider availability, Prod, or Cloudflare. |
| Application composition contract | The exact host invocation aliases and three `knowgrph.application.*` MCP tools resolve; exact revisions, interface and schema digests, negotiated connection capabilities, deterministic DAG order, immutable plan digest, owner delegation, and explicit migration diagnostics are required. | Treating `/`, `#`, or `@` aliases as wire methods; mutable references; absent capabilities; hidden executable, endpoint, or credential data; a second agent loop, registry, gateway, or proxy; silent upgrade, retry, migration, fallback, or deploy. |
| Repository packing contract | `/repository.pack #repository-packing @repository-root @runtime-proof`, `repository.pack`, and the single `knowgrph.repository.pack` tool resolve through original closed contracts; exact Knowgrph proof must cover canonical Git discovery, deterministic Markdown, typed omissions, source and artifact digests, containment, bounds, atomic replay, unchanged source, and zero network/model/token/cost. | Alternate aliases, remote repositories, external package/binary/service/network fallback, copied code or artifacts, source-byte MCP results, symlink or traversal escape, silent truncation, partial publication, model work, Prod, or Cloudflare. |
| Sandbox Agents runtime | Generic lifecycle, Docker compute, deny-first authorization, atomic checkpoint claims, independent local containment checks, immutable-image execution, loopback previews, snapshots, resume, redaction, bounds, cost, and cleanup are executable locally. | Copied provider SDK or client code, APIs, examples, prompts, schemas, fixtures, tests, model defaults, fabricated containment, host execution fallback, public ports, environment injection, online packages, or deployment. |
| Models and providers runtime | Revision-fenced provider registration, agent/run/process defaults, exact model features, independent transport selection, strict environment configuration, bounds, redaction, and fail-closed readiness are executable locally; one explicit model and transport also completed the approved proof. | Copied provider SDK code, model catalogs, examples, schemas, prompts, tests, fixtures, prose, fabricated capability evidence, default provider wiring, or unbounded live execution. |
| Mixture-of-agents contracts | `/moa`, matching `#` tags, matching `@` bindings, `moa.run`, and `agent.moa` resolve from source docs. | Copied MoA artifacts, hardcoded external provider presets, recursive aggregators, uncapped fan-out, or live provider execution. |
| Learning-loop contracts | `/memory.search`, `/experience.capture`, `/skill.propose`, `/skill.evolve`, `/identity.reflect`, matching `#` tags, and matching `@` bindings resolve from source docs. | External copied implementation artifacts, unreviewed self-modification, or live optimizer execution. |
| Skill Evolution ACOS contract | `/skill.evolve #skill-evolution @skill-catalog @skill-policy @runtime-proof @operator`, `SKILL-EVOLUTION.md`, and the validating `evolveSkill` transport resolve to one spec-complete, bounded, review-only contract. | Treating ACOS contract or transport checks as Knowgrph execution proof; SkillOpt implementation artifacts or dependencies; validation leakage; unreviewed apply; model-weight mutation; release; or deployment. |
| Stateful orchestration contracts | `/orchestration.graph`, `/state.checkpoint`, `/human.review`, `/stream.trace`, matching `#` tags, and matching `@` bindings resolve from source docs. | Copied graph runtime artifacts, hidden graph stores, unbounded cycles, or live external runtime execution. |
| Long-horizon SuperAgent contracts | `/superagent.run`, `#long-horizon-harness`, `#sandboxed-workspace`, `#message-gateway`, `@sandbox-workspace`, `@message-gateway`, and `superagent.run` resolve from source docs. | Copied DeerFlow runtime layouts, prompts, provider configs, examples, tests, fixtures, unbounded loops, or live external runtime execution. |
| Agentic video workflow | Dev runtime proves native model-backed script, character, storyboard, pending-shot render, checkpoint resume, landscape guard, context compaction, bounded retry, and an inspectable nine-stage multi-agent pipeline with typed handoffs, semantic assets, resource accounting, and dependency-propagated state. | Prod mirror writes, Cloudflare deploy, copied ViMax implementation artifacts, or uncontrolled paid calls. |
| KGC computing-flow | Docs declare `kgc-computing-flow/v1`, explicit handles, bounded execution, and KGC validation ownership. | Separate chat-local flow engine or direct graph mutation. |
| Cost and gates | Documentation validation is zero model spend; one separately approved three-call proof records returned usage and USD 0.00447 estimated cost while keeping mutation and deploy gates explicit. | Additional or repeated paid calls, payment, browser-auth, Prod mirror, or Cloudflare deploy. |
| Free-tier economics | Required GitHub checks keep their exact names and merge-queue triggers while no-op claims, dependency installs/caches, CodeQL TRAP caches, and unrelated dependency-security runs are filtered; dependency-free checks use Node 22 slim runners. Exact-head auto-delivery authorization is revoked after label or head drift. Cloudflare rejects invalid room upgrades before Durable Object access, applies rate-limit bindings before token issuance and valid room dispatch, defers empty-room persistence, reuses unexpired reconnect tokens, bounds durable retention and Worker CPU, caches alarm state, samples secret-safe custom logs at 1%, disables invocation logs and traces, hashes room ids in logs, and disables root and version preview endpoints. | Zero-cost or unlimited-capacity guarantees, live quota utilization, billing assertions, globally exact rate limiting, authenticated room creation, distributed-abuse resistance, Prod, or Cloudflare deployment. |
| Deployment boundary | Dev docs are changed locally only. | Prod mirror and Cloudflare runtime proof. |

## Proof Ledger

| VCC | Observable proof | Status |
|---|---|---|
| Frontmatter parses | Frontmatter command from `VALIDATION-RUNBOOK.md` reports `frontmatter ok` for every Markdown file in this folder. | Passed |
| Line budgets hold | `wc -l docs/*.md` reports every file under 600 lines. | Passed |
| ASCII holds | `LC_ALL=C rg -n "[^[:ascii:]]" docs` returns no matches. | Passed |
| Artifact scan holds | Split artifact pattern from `VALIDATION-RUNBOOK.md` returns no copied runtime artifacts. | Passed |
| Route consistency holds | Route consistency command reports `route consistency ok`. | Passed |
| Facts direct resolution holds | All `FACTS.md` direct-resolution command, semantic, and binding tokens are present in the matching dictionary frontmatter. | Passed |
| Probe-Tree clarification contract holds | `npm run probe-tree-contract:check` validates `PROBE-TREE.md` as the canonical owner, the structured action-topic set, semantic case-insensitive matching, 2-4 bounds, runtime-recognized selected-child terminal boundary, active Chat route, fail-closed no-hardcode/no-zero-model policy, and exact preset and dictionary projections. | Passed without provider execution |
| Automated collaboration and runtime identity gate holds | Startup proof on Knowgrph `8346579eecde318da0b156cfb05115ee3c9c6930` and Agentic Canvas OS/docs/catalog `e774aa8579a396972c815f477f8e6f51c151667d` passed the canonical owner/guest/worker harness with `2/2` isolated peers, fresh hydration in one attempt, remote document propagation, and common digest `48eb708312c33b6a24202c5a4fc69eb3b7858882f6a0e68f79822dc754bdd605`. The wrapper passed after isolating owner and guest apps from the canonical runtime; protected integration requires the same complete gate against the merged revisions. | Startup runtime gate passed; post-integration rerun required |
| Parallel collaboration gate sandbox holds | Candidate Knowgrph `b3aa08186156a60b23860a98ea897fd645446841` with canonical Agentic Canvas OS/docs/catalog `771959da6f8ac55a11e11e282f5dade01a7d9db9` passed `agentic-collaboration-gate-result/v2` on run-owned ports `15462`/`15463`/`15464`: `2/2` distinct authenticated peers, fresh hydration in one attempt, remote document propagation, and common digest `59b3a54155e58f33badf1a324e632f57a01b0776eb3e43492fa0358af8845479`. The run-owned listeners and persistence root were absent after cleanup; canonical `5173`/`8787` were never selected. | Candidate runtime gate passed; exact integrated cross-repository rerun required |
| Live GitHub cloud authority is executable | Protected source `ab76ef10e4ba1623d560ccc986eead76ad07b285` created ledger ref `agentic/collaboration-ledger`; main audit Check Run `90786821920` succeeded under GitHub Actions app `15368`. Claim `cff2ca8880864624b0e0b1e0e8244f717e4196d67ca84d8c3cfed673cb251ebf` advanced through claim `c653484dcb68734c0565d2a75140219cfa1c6c29`, bind `56ed6da8e00ba32e043dd357cafad375cbd5144a`, heartbeat `3fcf19e0d0e1171497239ac09894a8e2dcc76fe2`, review-ready `f653c56ede84cf39b29007be5159759daf64dd3f`, and abandoned release `c47457c7a677f01079d3d168e8db93aeb6c1ee0c` without an integration receipt. Read-only verification receipt `96457f960172c46c8432e16106f279d2ddf793a33463839a4ceb9133ccd53fd1` bound historical PR `#184` head `9d5376237f19c2d6a12713eaef163493d458e577`, review-ready digest `60bc82edf3ac641a85d7df4ed87352f3b0d00597f3eac8afc5f2fbbb8fbacd77`, write-set digest `441b0d6dfe010bd02600baf011e3f6afee1ac39c3e484c1d6cda259377097098`, and zero findings. Exact-head Check Run `90787704139` succeeded; after release, Check Run `90788006952` failed closed on the same historical head. Read-only workflow dispatch `30516900541`, job `90788690383`, then returned current ledger revision `19d5825c2ad5e9390d108f08c03bd0662b03823e` through the browser-compatible Actions route. Main protection requires the app-bound check strictly with admin enforcement; Actions allow only SHA-pinned GitHub-owned actions with read-only workflow defaults; ruleset `20021161` prevents ledger deletion and non-fast-forward updates with no bypass. | Runtime-ready for GitHub-hosted Agentic Canvas OS Dev coordination at recorded revisions; physical second-device/mobile execution, private cross-repository writes, merge-group proof, Prod, and Cloudflare remain gated |
| Multi-worktree writer coordination is executable | Focused Node tests exercise atomic Git-common-directory lease registration, distinct-worktree parallel claims, same-worktree exclusion, TTL renewal, expired-session epoch takeover, exact recovery, fencing, pull-request metadata, different-scope coexistence, duplicate-scope rejection, and redacted output. The lifecycle suite additionally passes 7/7 cases proving attributed untracked paths remain cleanup-ineligible `owned-untracked` state in their physical lane, keep unrelated scopes ready, and leave canonical or unattributed dirt blocked. | Runtime-ready in Dev; protected CI is required for every revision |
| Legacy clean committed lane bootstrap is executable | `npm run workspace:legacy-clean-bootstrap:check` reports 18/18 focused checks across exact-head/tree preservation, local-path redaction, completed replay, interruption after each of six external boundaries, single-effect recovery, dirty and unregistered lanes, scope and write overlap, invalid write scope, committed-path drift, unattributed projections, immutable-head drift, and incomplete adapters. The provider-neutral CLI loads a caller-selected adapter while the core pins repositories, session, device, branch, worktree registration digest, base, head, tree, changed paths, normalized write scope, identity, per-phase receipts, and checkpoint lineage. | Contract runtime-ready in Dev; no real legacy lane, cloud provider, review provider, push, merge, deployment, or publication was exercised |
| Managed implementation lifecycle is executable | Focused Node tests exercise safe provision from exact clean canonical main, collision and symbolic-link rejection, registry-fenced rollback, one-object JSON lifecycle results, authoritative draft/review projections, exact review replay and fenced resumption, and durable park. The canonical local-runtime suite rejects dirty or noncanonical sources, incomplete protected checks, malformed state, and stale ownership tokens. It also proves an atomic exact-session handoff from token-owned Vite to canonical storage and Apex, while wrong-session or changed process evidence blocks before termination. Acceptance additionally requires fixed loopback Apex/storage listeners plus direct and proxied HTTP readiness. | ACOS runtime-ready in Dev; protected integration and live supervisor acceptance remain separate claims |
| Agentic SDLC conformance is executable | `npm run agentic-sdlc:check` proves fence-aware v1.7 guideline parsing, one-based Rule IDs with retained rule text, the exact authoring/execution finding union and severities, deterministic zero-count reports, authoring/mirror/delivery evidence rungs, the canonical task state machine, VCC coverage, acyclic and write-disjoint planning, independent Evaluator verdicts, exact property/check receipts, structured result counts and typed measurements, immutable grants and four budget caps, stage-specific guideline loads, repeated-failure and Operator gates, joined partial-state recovery, three distinct Dev trust roots, whole-ledger persistence signatures, input immutability, and a valid end-to-end `agentic-sdlc-run/v1` artifact. `npm run agentic-sdlc:source:check` binds the actual v1.7.0 and v1.0.0 guideline bytes to the recorded revision and SHA-256 digests. | Runtime-ready in the Dev authoring lane; Knowgrph projection must integrate at the exact protected revision before the combined runtime claim |
| Agentic SDLC admission adapter is repository-owned and fail-closed | `npm run lifecycle:conformance -- --evidence=<path>` accepts only `agentic-sdlc-admission-evidence/v1`, resolves immutable evaluator and schema closure digests from exact Git objects at the tracked clean Agentic Canvas OS `HEAD`, and evaluates structured pre-dispatch baseline attestation, complete VCC-task closure, positive numeric dependency-ordered wave ordinals, budgets, two-failure circuit breakers, intended-use capability grants, an unexpired collaboration fence with a complete active-writer inventory on distinct worktree, branch, and semantic-scope lanes with non-overlapping declared scopes at the explicit evaluation time, complete dependency admission, and a named independent Evaluator. Exactly five producer contracts are permitted: `admission:authoring-baseline`, `admission:task-plan`, `admission:collaboration`, `admission:dependency-admission`, and `admission:execution-evaluator`; each operation must carry a coherent recorded terminal result rather than a status assertion alone. The evaluator validates these records but does not authenticate caller transport provenance or resolve caller-supplied source tuples to Git objects; only a later protected consumer adapter can create authoritative managed-run evidence. Exit `0` verifies the supplied admission contract, exit `1` emits its typed blocked form, exit `2` reports evaluator or schema failure without a domain verdict, and exit `3` reports unavailable or mismatched policy, evaluator, schema, source, or dependency identity. The receipt binds policy, evaluator, schema, source, dependency, input, finding-set, stage-evidence, and receipt digests, enforces only `admission`, and leaves the seven successor stages unevaluated. | Deterministic fixture verification is contract proof only. The raw current Knowgrph observation remains identity-unavailable and unevaluated until a later protected producer resolves and authenticates the complete input; no managed-run review, integration, runtime, candidate, release, deployment, publication, public, Prod, or Cloudflare claim |
| Agentic SDLC observability catalog is independently checkable | `node --test __tests__/agentic-sdlc-observability-contract.test.mjs` checks exact canonical tokens, receipt and response identities, complete graph vocabulary, stable ordering, typed `verified`/`delivery_ready`/`deployed` boundaries, existing-owner reuse, zero economics, planning, and Dev-only closure. | ACOS documentation-contract evidence only; matching Knowgrph source/runtime proof and exact protected revisions remain separate |
| Application composition contract is executable | `npm run application-composition-contract:check` validates the canonical alias tuple, three exact MCP tool ids, exact revision and schema fencing, deterministic immutable plan semantics, connection capability negotiation, opaque integration profiles, owner-preserving bounded sequencing, non-automatic migration diagnostics, and forbidden loop/proxy/retry/deploy behavior. | ACOS contract runtime-ready; Knowgrph local MCP fixture proof and protected integration remain required for the combined runtime claim |
| Repository packing contract is executable | `npm run repository-packing-contract:check` validates the exact `/`, `#`, `@`, skill, and MCP identities, closed request/result, hard bounds, owner split, deterministic grammar, fail-closed safety, content-addressed atomic publication, and clean-room dependency/reserved-name guards; a separate provenance and similarity review remains required and is not automated. | ACOS contract runtime-ready; exact Knowgrph contract/runtime/stdio/independence proof and protected docs pin remain required for the combined runtime claim |
| Repository runtime-readiness source audit is executable | `npm run repository-runtime-readiness:check` proves exact Git revision fencing, metadata-first bounded discovery, package-manager and generated-input drift findings, environment-value exclusion, layer separation, stable recommendations, zero model/provider/paid/token cost, and unchanged mutation/network/integration/release/deployment boundaries. The exact Builders Hub reference snapshot `c7714b94b0d7ed2c259d9cbbb272792aa862c12f` exits `1` with source admission blocked and every higher layer unverified. | Runtime-ready for read-only source admission; the reference subject remains blocked, the upstream guideline candidate is not protected, and local build, browser, integration, deployment, Prod, and Cloudflare proof remain gated |
| AI Voice Studio ACOS contract is executable | `npm run voice-studio-contract:check` validates one `/voice.studio` command, three exact semantic routes, route-specific shared bindings, one `knowgrph.voice.studio` wire identity, consent and approval separation, rights, revocation, disclosure, provenance, bounds, clean-room source guards, and zero-model evidence. | Spec-complete ACOS contract only; exact integrated Knowgrph MCP, deterministic adapter, persistence/read-back, idempotency, cancellation, and live-configuration failure proof remain required |
| Cache context is executable | `npm run cache-context:check` reports 6/6 passing for exact stable-prefix reuse, idempotent registration, revision invalidation, least-recent bounded eviction, provider-eligibility honesty, and cache read/write telemetry. `GET /api/ready` exposes only sanitized registry policy and keeps provider cache status `unverified`. | Runtime-ready in Dev; live provider hit remains gated |
| Reasoning continuity is executable | `npm run reasoning-continuity:check` reports 8/8 passing for first-turn behavior, exact invariant reuse, drift reset, capability gating, returned-context confirmation, mismatch rejection, active-turn serialization, and bounded retention. `GET /api/ready` exposes only policy and counters while provider-effective context remains `unverified`. | Runtime-ready in Dev; live provider confirmation remains gated |
| Function calling is executable | The focused gateway suite covers the controller, durable continuation, execution-receipt, MCP transport, OpenAI adapter, guarded gateway, Dev deployment, and sanitized live-proof composition owners. `npm run function-gateway:cross-repo-proof -- --knowgrph-root=<checkout>` additionally runs the real Agentic receipt owner against Knowgrph's native run-note Durable Object: a dropped first receipt blocks retryably, the second call returns `replayed`, the manifest remains revision 1, and a terminal Agentic replay makes no third MCP call. The accepted `LIVE-REVIEWED-FUNCTION-PROOF.md` record resumed one already-paused durable continuation, completed one logical provider run in two Responses requests and one reviewed call, returned 546 input and 55 output tokens at USD 0.000876 estimated cost, matched completed application and applied native receipts, and read back exact revision 1. `read_agentic_os_status` stays read-only; `update_agent_run_note` remains intrinsically review-required and maps only to `knowgrph.run_manifest.note.update`. | Verified bounded live in Dev; one reviewed native mutation; Prod remains gated |
| Agent definitions are executable | `npm run agent-definitions:check` reports 8/8 passing for exact source URI and SHA-256 verification, immutable minimal packets, authorized optional behavior, reference-only capability denial, handoff target verification, revision conflict and stale fencing, unknown-field and capacity rejection, explicit bounds, and text or structured output validation. App and Worker tests expose sanitized registry policy while no definitions are registered. | Runtime-ready in Dev; proof fixtures executed live while default definitions remain empty |
| Models and providers are executable | `npm run model-providers:check` reports 7/7 passing for process defaults, agent/run/process precedence, provider-local fallbacks, independent transport matching, exact features, missing routes, revision conflicts, capacity, removal, strict neutral environment fields, endpoint safety, legacy-alias rejection, and secret redaction. App and Worker tests keep default provider execution `unverified`. | Runtime-ready in Dev; one explicit model/transport passed bounded live proof |
| Running agents is executable | `npm run running-agents:check` reports 7/7 lifecycle checks and `npm run durable-agent-state:check` reports 3/3 atomic state checks for transitions, exclusive continuation, streaming, same-turn pause, cross-isolate resume, competing-claim fencing, step and event bounds, timeout, and honest cost. App and Worker tests keep adapter execution false while exposing durable binding state. | Runtime-ready in Dev; durable recovery proven offline and continuation has bounded live proof |
| Guardrails and human review are executable | `npm run guardrails-human-review:check` reports 9/9 guardrail and durable-state checks for ordered transformations, four stages, signed reviewer evidence, invalid-evidence retry, approve/reject/edit, atomic consumption, expiry, capacity, and cross-isolate pause recovery. The Function Gateway suite proves the concrete guarded status path pauses and executes once after exact signed review. | Runtime-ready in Dev for durable offline review; live reviewed side effects remain gated |
| Agent orchestration and handoffs are executable | `npm run agent-orchestration:check` reports 8/8 passing for exact topology, explicit branch ownership, source-owned manager delegation, target-owned conversation handoff, owner-only continuation, specialist delegation, manager handback, authorization before execution, revision fencing, concurrency, replay, intermediate-output suppression, partial cost, and error redaction. App and Worker tests keep configuration false. | Runtime-ready in Dev; manager delegation and specialist handoff passed bounded live proof |
| Agent Swarm is executable | `npm run agent-swarm:check` reports 34/34 passing across exact-agent resolution, goal-only planning, caller role/task/workflow and signal rejection, session ownership/lifetime, ABA-safe reservation cleanup, observed overlapping intervals and single-worker fallback, DAG limits, admission-fixed deadlines, full-lease claim admission, Durable Object atomic claims, cross-runtime recovery, stable retry idempotency, stale fencing, verified receipts, task/synthesis cancellation, partial synthesis, sanitized readiness, and unconfigured fail-closed behavior. No provider call or external runtime dependency is present. | Runtime-ready offline in Dev; live adapters, Prod, and Cloudflare remain gated |
| Agent Toolkit is executable | `npm run agent-toolkit:check` passes the combined Toolkit/runtime/app/Worker/durable-state suite for arbitrary adapter wrapping, server-owned timing and signals, lifecycle fencing, metadata redaction, structured exporter failure isolation, p50/p95/p99 profiling, bounded bottleneck identification, caller-declared digest consistency, application authorization, per-principal request/run/cohort quotas, poisoning and duplicate-evidence rejection, stable evaluator idempotency, one-owner cost, deterministic thresholds and multi-candidate optimization, immutable review-pending proposals, principal-derived isolation, remote-evidence exclusion, Durable Object claims and alarms, and sanitized readiness. No provider call, external dependency, prompt/parameter mutation, or apply method is present. | Runtime-ready production contract; production readiness becomes true only with durable state, telemetry, an application authorizer, and an injected evaluator. Measured improvement, protected integration, Prod release, and Cloudflare deployment remain gated |
| Agent runtime composition is executable | `npm run agent-runtime-composition:check` reports 12/12 passing for real source hashing, exact source preparation, provider, model, transport, feature, and definition selection, one Running Agents lifecycle, previous-response continuation, guardrail boundaries, output rejection and reset, drift blocks, explicit ownership, hidden intermediates, and honest cost. App and Worker tests keep the default adapter unconfigured. | Runtime-ready in Dev; one explicitly approved composed live proof passed |
| Opt-in autonomous runtime is executable | `npm run autonomous-runtime:check` reports 28/28 passing across explicit enablement and spend gates, source SHA-256 mismatch, model alignment, provider-call bounds, source-verified registration, authenticated Worker routing, strict caller fields, principal-scoped continuation, previous-response reuse, returned usage cost, readiness redaction, and zero provider requests while unconfigured. The full `npm run check` reports 464/464 tests plus build and docs success. | Runtime-ready offline in Dev; disabled by default, live provider behavior and deployment remain unverified |
| Progressive Agents is executable | `npm run progressive-agents:check` reports 4/4 passing for one exact direct agent run, one tool-bearing definition routed through the real local Function Calling controller and gateway seam, one manager-owned specialist delegation, missing-owner blocks, and strict public request fields. App and Worker readiness expose the progression while keeping default execution unconfigured. | Runtime-ready in Dev; offline gateway proof only, with provider execution unverified |
| Bounded live agent provider proof is executable | `npm run agent-live-provider:check` reports 5/5 passing. The separately approved `npm run agent-live-provider:proof` invocation completed exactly three `gpt-5.6-sol` Responses calls on its first run: two for manager-owned delegation and one for specialist-owned handoff. The returning specialist reused its hash-linked stored response and the provider confirmed effective `all_turns`. Returned usage was 576 input and 53 output tokens with zero cached-token hits and USD 0.00447 estimated cost at the supplied price snapshot. Output retained only source, response, and answer digests; no key, raw response id, model output, or reasoning was emitted. | Verified bounded live in Dev; default Worker, Prod, and Cloudflare unchanged |
| Sandbox Agents is executable | `npm run sandbox-provider:check` reports 13/13 controller/provider tests. With a healthy local Docker Engine and caller-supplied immutable image digest, `npm run sandbox-docker:check` reports 20 independent configuration and behavioral checks, real file and argv work, offline local package installation, internal agent networking, loopback preview proxy traffic, snapshot seeding, atomic cross-controller resume, zero reported cost, and zero remaining labeled containers or networks. App and Worker tests keep the unconfigured default honest. | Runtime-ready for bounded local Docker execution; default Worker hosting and formal third-party containment certification remain gated |
| Programmatic tool calling is executable | `npm run programmatic-tool-calling:check` reports 12/12 passing for hosted orchestration, stored continuation, complete ordered stateless replay, opaque reasoning and fingerprint handling, exact function caller lineage, direct route selection and enforcement, schemas, bounds, serialization, timeout, costs, no local dynamic-code fallback, and no generated-code return. App and Worker readiness tests expose only sanitized policy while provider context isolation remains `unverified`. | Runtime-ready in Dev; hosted adapter proof remains gated |
| Tool Search is executable | `npm run tool-search:check` reports 8/8 passing for metadata-only initial exposure, immutable prefix state, private hosted catalog mapping, exact client loading, canonical hosted validation, programmatic preloading, capability and adapter gates, namespace and result bounds, replay rejection, timeout, session cleanup, returned or unreported cost evidence, and no authorization of unselected tools. App and Worker tests expose sanitized policy while provider context reduction remains `unverified`. | Runtime-ready in Dev; live search and context-reduction proof remain gated |
| Instruction audit is executable | `npm run instruction-audit:check -- --baseline-ref=9916527fb7ac78dbd80772eaa7412c21259314de` reports 7/7 focused tests, two passing current surfaces, 1,086 body words, 4,102 estimated tokens, zero violations, and 58,143 fewer characters (78%) than the inspected base. Fixtures prove missing intent, duplicate directives, excessive directive context, embedded procedures, canonical-owner leakage, and route drift fail closed. | Runtime-ready in Dev; quality evaluation is separately owned |
| Instruction task-quality evaluation is executable | `npm run instruction-quality:check` validates four bounded final-answer cases and reports 7/7 tests for complete behavior, missing owner intent, unsafe deploy advice, excess length, case drift, evaluator model-use honesty, private-reasoning exclusion, and unchanged deploy state. | Harness runtime-ready in Dev; no named model candidate or general quality claim |
| Soul routes hold | Route consistency checks include `/soul.load`, `/personality.overlay`, `#soul`, `#primary-identity`, `#personality-overlay`, `@soul-profile`, `@identity-slot`, `@personality-overlay`, `soul.load`, and `personality.overlay`. | Passed |
| Persistent memory routes hold | Route consistency checks include memory write, compact, search, session search, user profile, target tags, bindings, and skill contracts. | Passed |
| Memory-log compliance holds | The structural command validates `memory-log/v1`, UTC-valid `YYYYMMDDTHHmmssZ`, shard-month identity, unique ordered sigils, and required fields; the base-ref command proves historical bytes remain an unchanged prefix. | Passed |
| Skill-system routes hold | Route consistency checks include skill discovery, load, bundle, manage, progressive-disclosure tags, open-standard tag, security tag, bindings, and skill contracts. | Passed |
| Context-file routes hold | Route consistency checks include context discover, load, audit, context tags, working-directory bindings, policy bindings, and skill contracts. | Passed |
| Context-reference routes hold | Route consistency checks include reference expand, audit, context-reference tags, reference bindings, attached-context packets, and skill contracts. | Passed |
| Kanban routes hold | Route consistency checks include Kanban task, handoff, sync, board tags, row bindings, profile bindings, worker process bindings, and `kanban.collaborate`. | Passed |
| Centralized planning compliance holds | Startup validates the bounded index, legacy identities, and all Context records; release validates one new immutable record for the declared Context. | Passed |
| Context-record compliance holds | Structural validation proves unique Context ownership and deterministic projection; release rejects legacy mutation, base-existing records, and extra planning paths. | Passed |
| Tools and toolsets routes hold | Route consistency checks include toolset enable/disable, tool-function tags, toolset tags, platform-toolset tags, bindings, and skill contracts. | Passed |
| Tool-gateway routes hold | Route consistency checks include tool catalog, route, provider select, audit, tool-category tags, bindings, and skill contracts. | Passed |
| Tool-search routes hold | Route consistency checks include tool search, describe, call, deferred-schema tags, bridge bindings, memory contracts, and skill contracts. | Passed |
| MoA routes hold | Route consistency checks include `/moa`, `#mixture-of-agents`, `#reference-agents`, `#aggregator-agent`, `@moa-preset`, `@reference-agents`, `@aggregator-agent`, `moa.run`, and `agent.moa`. | Passed |
| Learning-loop routes hold | Route consistency checks include learning commands, tags, bindings, `skill_contracts`, and `agent.learning`. | Passed |
| Skill Evolution ACOS contract and transport check | `npm run skill-evolution:check` runs model-free contract, clean-room dependency/import, and fail-closed MCP-result tests for exact invocation, resumable operations, source and mini-batch gates, isolated validation, strict promotion, complete snapshots, and safety flags. | Passed for spec and transport only; no Knowgrph runtime-readiness claim |
| Role-based Agent Team local Dev proof | Agentic Canvas OS `npm run agent-team:check` validates the canonical contract. Matching Knowgrph `npm run agent-team:check` passes contract, lifecycle, integrity, hardening, clean-room, local-host, and canonical stdio execution proof for the exact built-in manager/evidence/risk team, zero-cost settlement, and no-call replay. | Passed for local Dev; exact protected cross-repository integration remains required |
| Stateful orchestration routes hold | Route consistency checks include orchestration commands, tags, bindings, `skill_contracts`, and `agent.orchestrator`. | Passed |
| Agent Swarm routes hold | Route consistency includes `/agent.swarm`, `#agent-swarm`, `@swarm-run`, and `agent.swarm`; direct facts resolution and the clean-room source boundary are explicit. | Passed |
| Agent Toolkit routes hold | Route consistency includes `/agent.toolkit`, `#agent-toolkit`, `@agent-toolkit-observer`, and `agent.toolkit`; direct facts resolution and the clean-room source boundary are explicit. | Passed |
| SuperAgent routes hold | Route consistency checks include `/superagent.run`, long-horizon tags, sandbox/message bindings, and `superagent.run`. | Passed |
| External copy guard holds | Artifact and text scans include external-copy indicators and return no copied code, APIs, prompts, schemas, examples, tests, fixtures, or prose from referenced agent repositories. | Passed |
| Computing-flow is canonical | `SKILLS.md`, `DICTIONARY-COMMAND.md`, and `DICTIONARY-SEMANTIC.md` expose `flow.computing`, `/computing-flow`, and `#computing-flow`. | Passed |
| Slash dictionary has external proof | `npm -C $KNOWGRPH_ROOT/canvas run test:ci:unit -- ui.floatingPanelChat.composer.memoryInvocationRuntime` reports `SUMMARY total=1 ok=1 failed=0`. | Passed |
| FloatingPanel Chat action recommendation has external proof | Focused tests for `ui.floatingPanelChat.pipeline`, `ui.floatingPanelChat.quickActions.invocationRoutes`, `ui.floatingPanelChat.contextRail.quickActions`, `ui.floatingPanelChat.composer.ingestCommandRegistry`, `ui.floatingPanelChat.composer.slashVariableMenus`, and structured prompt contracts report `SUMMARY ... failed=0`. | Passed |
| KGC computing-flow has external proof | `npm -C $KNOWGRPH_ROOT/canvas run test:ci:unit -- chat.responseContract.prompt.kgcComputingFlowKtvShape` reports `SUMMARY total=1 ok=1 failed=0`. | Passed |
| Agentic video workflow has Dev proof | Combined multi-agent-pipeline, parallel-shot-generation, image-consistency, automated-image-generation, reference-selection, multi-camera, expressive-storyboard, long-script, narrative/continuity, workflow, live-client, runtime, cost, property, local MCP, and remote MCP suites report 136/136 passing. This includes the forward-only nine-stage DAG, specialist assignments, typed handoffs, semantic checkpoint reuse, selected-frame/clip/output indexing, provider economics, honest unverified completion, dependency-propagated blocking, same-camera concurrency, stable output ordering, VLM selection, temporal state, and accounting proof. Changed-file hygiene remains blocked by unrelated oversized shared-checkout files, and the Canvas TypeScript gate is separately blocked by an unrelated `flowCanvasOverlayNativeScenePartitionRegression` GraphData fixture missing its required `type`. | Passed with unrelated shared-checkout gates reported |
| Central prompt-preset selection and execution boundary have Dev proof | The source-backed `PROMPT-PRESETS.md` catalog exposes Video Agent, SME Care Agent, Investment Research Agent, and Crawler Agent in the first-class FloatingPanel Prompt Presets view. Each row uses a catalog-owned `/*-prompt-preset` selection alias and resolves a separate executable `/...-agent` runtime route; selecting a row loads the centralized prompt without submitting or adding chat history. Focused catalog and invocation tests prove four-route loading, alias-to-runtime resolution, duplicate or missing-entry failure, native crawler routing, and the absence of the stale video-only control. Existing video preset, persistence, media-projection, and deterministic output tests continue to prove the Video Agent path, while crawler proof covers the native Import URL workflow and separate persisted report and Markdown pipe-table Canvas outputs. Send remains the execution boundary, and validation made no Prod mirror write, Cloudflare action, or historical artifact backfill. | Passed |
| Import URL invocation is canonical in Agentic Canvas OS | `npm run url-ingest-contract:check` validates one `/ingest-url` command, existing `@url:` and `@reference-policy` bindings, existing `#canvas`, one `url.ingest` skill, the guarded browser executor, read-only discovery, and forbidden aliases. | Passed for source metadata and model-free contract validation; integrated Knowgrph browser execution and persistence proof remain separate |
| Image to Three.js skill has Dev proof | Eight focused conversion, projection, source-replacement, fallback, and disposal selectors pass; the browser-smoke contract passes; task-mode browser proof renders a PNG Rich Media Panel, runtime-generated JPEG Card, SVG Rich Media Panel, SVG Storyboard Widget, and typed fallback on the visual Canvas. Canvas TypeScript and hygiene gates pass, package manifests remain unchanged, and no external plugin code or dependency is present. | Passed |
| Procedural image-to-3D quality contract has Dev proof | Knowgrph reports 33 focused `imageToGlb` selectors plus Canvas TypeScript, focused ESLint, hygiene, chunk-budget, and documentation-lint passes. The source-tested contract covers connected contour volumes, compact construction budgets, observed-front versus inferred-hidden provenance, rigid named pivots and sockets, one four-second +/-12-degree loop clip, separate geometry, material, reference, and action gates, and GLB plus editable external-buffer glTF export without an external runtime, model, or dependency. | Passed within the Dev source-test boundary; live provider execution, browser fidelity, deformable skinning, Prod, and Cloudflare remain unverified |
| Canonical docs source has Dev proof | The registered Agentic Canvas OS `main` worktree remains the only normal runtime docs source and must be clean at fetched `origin/main`. Focused coordination tests prove additional registered task worktrees can mutate branch-exclusive scopes without becoming runtime sources. Knowgrph consumer predev alignment with this v2 policy remains a separate downstream gate. | Passed for Agentic Canvas OS; downstream consumer alignment pending |
| Deploy guard holds | Scoped git status shows no `content/knowgrph` mutation and no deploy command was run. | Passed |
| Neutral collaborative human-authorized lifecycle contract holds | `scripts/collaborative-release-lifecycle-contract.mjs` and its focused suite prove the normative protocol is provider-neutral; parallel actors, devices, sessions, and worktrees require complete identities and current fences; immutable Overlap Preservation and Disposition Receipts bind every pre-existing work item to its owner, write set, state, recovery handle, overlap class, and retained-or-restored outcome before the Integration, Runtime Review, Candidate, Human Authorization, Deployment, State Reconciliation, Live Verification, Publication, and Rollback chain; overlapping work cannot be auto-restored; exact duplicate target dispatches coalesce while competing candidates fail; and preservation, source, transitive dependency, policy, target, artifact, manifest, receipt, expiry, replay, authorization, authoritative-state, immutable-origin, public-route, and client-cache drift fail closed. The Knowgrph profile maps worktrees, durable recovery refs, protected integration, controlled review, production authorization, deployment, authoritative state readback, immutable-origin and public-route verification, browser and returning-client convergence, mirror publication, rollback, and ownership-safe cleanup as replaceable reference adapters. | Twenty-five focused lifecycle cases and the 60-file documentation contract passed for universal guideline v1.11.0 and canonical lifecycle schema v6. This is documentation and executable contract evidence only; protected integration, a new deployment, state mutation, public-route verification, publication, Prod, and Cloudflare were not performed by this change. |
| Free-tier economics source contract holds | Focused lifecycle, Worker preflight, AgentState, and CanvasRoom tests prove four required CI check names remain present on Node 22 slim runners, CI restores no dependency cache, source and dependency security have separate triggers, exact-head auto-delivery authorization is revoked on drift, invalid or rate-limited canvas requests make zero Durable Object namespace calls, empty rooms make zero storage/alarm writes, healthy reads make zero storage writes and reuse cached alarm state, durable retention is bounded, ordinary activity does not reset an existing alarm, early alarms retain the exact idle deadline, and structured logs omit raw room capabilities. Wrangler 4.113.0 root and explicit Dev dry runs both resolve the two rate-limit bindings and bundle one asset at 628.10 KiB raw and 122.54 KiB gzip without deployment. | Dev source and bundle proof only; live quota, billing, deployment, globally exact admission, authenticated room creation, Workers-runtime hibernation semantics, and production-route behavior remain unverified |
| Invocation catalog digest parity has focused Dev proof | `npm run docs:check`; the managed-implementation, instruction-audit, and production-readiness contract tests; Knowgrph's MCP docs runtime test; Canvas TypeScript; revision rollover, retry, provenance, digest-drift, source-metadata precedence, FloatingPanel projection, and runtime-identity attestation selectors; and the cross-repo dictionary consumer test passed. The source catalog contains 139 commands, 139 semantics, and 133 bindings with SHA-256 digest `a852ec74a5ee08a4723b2dc5f9d4938c701d38c7e62abdabe8c4b92f62b23d6e`. | Passed in isolated Dev task lanes; protected integration and deployment remain separate |
| Workspace artifact lifecycle contract is source-backed | The command, semantic, binding, skill, and MCP catalogs define one provider-neutral plan/apply contract for bounded inspect, create, update, import, export, trash, and restore operations. The publishing lane preserves byte-identical Markdown imports, and the Knowgrph lane owns focused runtime and stdio proof. | Local Dev evidence only; protected integration, recursive folder transfer, purge, provider sync, publication, Prod, and Cloudflare remain separate |

## Promotion Boundary

| Claim | Status | Reason |
|---|---|---|
| Agentic Canvas OS docs control surface | Runtime-ready | Parse, route, scan, proof, and deploy-boundary checks are reproducible locally. |
| Cloud-authoritative writer coordination | Runtime-ready in GitHub-hosted Agentic Canvas OS Dev | Protected main, a live ledger lifecycle, exact-head success and post-release failure, app-bound strict protection, no-bypass ledger rules, and a browser-compatible Actions dispatch are recorded above. The proof does not claim physical second-device/mobile execution, private cross-repository writes, merge-group verification, integrated release, Prod, or Cloudflare. |
| Reviewed delivery authorization | Runtime-ready in focused local Dev proof | The provider-neutral ledger now separates `review-ready` from `delivery-authorized`; focused tests prove exact reviewed-head/evidence binding, operator and protected-integration intent digests, idempotent replay, stale-evidence and edit-after-review rejection, verification before auto-merge, and reviewed-lane integration without reopening authoring. Live protected integration, deployment, Prod, and Cloudflare remain separate. |
| Reviewed forward-child recovery | Runtime-ready in focused local Dev proof | Eleven focused tests prove exact authorization, adaptive recovery joins, replay, response-loss reconciliation, no-force CAS publication, per-operator journal generations, preservation of content-valid completed legacy journals, malformed-history rejection, and branch-wide exclusion of incomplete competing generations. The historical PR #368 receipt remained unchanged during diagnosis; protected integration and a fresh recovery authorization remain separate gates. |
| Adaptive lifecycle monitoring | Runtime-ready in focused local Dev proof | Twenty-three model-free tests prove provider-neutral normalization, exact target matching, request-bound heartbeat baselines, content-bound replay, resume-signal and schedule semantic rebinding, explicit rejection of fixed expiry, deterministic bounded backoff and jitter, progress reset, replay polling that consumes budgets without zero-delay loops, typed error policy, identity and monotonicity guards, cancellation interleaving safety, elapsed-budget cancellation and late-result rejection, restartable checkpoints, sequential reads, bounded regular-file JSON input, and `mutationAuthority: false` on every result and resume signal. The monitor never renews a claim or infers completion from silence, expiry, timeout, process success, or a model/tool response; protected integration, live-provider adapters, Production, and deployment remain gated. |
| Invocation catalog digest parity | Runtime-ready in Dev | The docs MCP emits exact full-catalog counts and one deterministic SHA-256 digest; browser hydration replaces all three sigil slices, recomputes the digest, exposes it in FloatingPanel and runtime identity, preserves source metadata on token collisions, and fails stale or blocked on drift. Protected integration, Prod, and Cloudflare remain gated. |
| Soul identity docs contracts | Runtime-ready for docs | Contracts are route-complete, source-backed, scan-bounded, no-copy, and no-hardcoded-default; live prompt runtime remains separately gated. |
| Persistent memory docs contracts | Runtime-ready for docs | Contracts are route-complete, bounded, target-separated, scan-gated, capacity-aware, and no-copy; live memory runtime remains separately gated. |
| Skills system docs contracts | Runtime-ready for docs | Contracts are route-complete, metadata-first, progressive, resource-bounded, scan-gated, open-standard-compatible, and no-copy; live skill runtime remains separately gated. |
| Instruction audit Dev runtime | Runtime-ready in Dev | The local audit is deterministic, intent-preserving, budgeted, duplicate-aware, canonical-owner-aware, baseline-comparable, zero-cost, and non-mutating; it does not claim model-quality improvement from structural reduction alone. |
| Instruction task-quality evaluation | Runtime-ready in Dev | The suite and scorer are executable, model-agnostic, provenance-bound, final-answer-only, and fail closed; model-specific quality remains unproven until a complete candidate passes and receives human review. |
| Context files docs contracts | Runtime-ready for docs | Contracts are route-complete, working-directory-scoped, precedence-aware, scan-bounded, subordinate to facts and identity, and no-copy; live context runtime remains separately gated. |
| Context references docs contracts | Runtime-ready for docs | Contracts are route-complete, workspace-scoped where applicable, egress-aware, warning/refusal typed, bounded, and no-copy; live reference runtime remains separately gated. |
| Kanban collaboration docs contracts | Runtime-ready for docs | Contracts are route-complete, row-based, profile-named, worker-process-aware, conflict-aware, shared-utility-owned, and no-copy; live worker runtime remains separately gated. |
| Tools and toolsets docs contracts | Runtime-ready for docs | Contracts are route-complete, schema-backed, existing-function-only, platform-scoped, approval-gated, secret-safe, and no-copy; live toolset runtime remains separately gated. |
| Tool Gateway docs contracts | Runtime-ready for docs | Contracts are route-complete, existing-infrastructure, per-tool, approval-gated, cost-logged, secret-safe, and no-copy; live tool runtime remains separately gated. |
| Function calling Dev runtime | Runtime-ready offline in Dev | The local controller and native Knowgrph run-note owner prove reviewed mutation, atomic receipt deduplication, uncertain-result recovery, and terminal replay with zero provider calls; live model, remote Worker, Prod, and Cloudflare execution remain separately gated. |
| Tool Search Dev runtime | Runtime-ready in Dev | The local controller is typed, bounded, session-scoped, metadata-first, exact-load, caller-aware, cost-aware, fail-closed, and no-copy; live provider search, context reduction, and downstream gateway execution remain separately gated. |
| Programmatic tool-calling Dev runtime | Runtime-ready in Dev | The local controller is typed, bounded, caller-linked, cost-aware, fail-closed, and forbids local code execution; a live hosted adapter, exact model capability, and provider context-isolation evidence remain separately gated. |
| Running agents Dev runtime | Runtime-ready in Dev | The local controller is typed, bounded, continuation-exclusive, pause-aware, same-loop streaming, cost-honest, fail-closed, and no-copy; a downstream adapter and live provider, tool, handoff, session, and streaming proof remain separately gated. |
| Agent runtime composition Dev runtime | Runtime-ready in Dev with bounded live proof | Exact source verification, model and transport selection, Running Agents continuation, definition-owned output validation, and manager/specialist ownership transitions passed one explicitly approved three-call provider run. The shipped default still has no execution adapter or authorizer; the autonomous adapter is injected only under complete opt-in configuration. |
| Application composition contract | Runtime-ready for ACOS contract | Exact host aliases, strict MCP schemas, negotiated capability evidence, immutable digest-bound planning, deterministic dependency order, opaque integration profiles, bounded owner delegation, and explicit non-mutating migration diagnostics are defined and focused-tested; combined Knowgrph execution remains gated by its local MCP proof. |
| Agentic SDLC observability catalog | Spec-complete for ACOS contract | The exact invocation, one local wire-tool identity, immutable receipt gate, deterministic graph vocabulary, existing KGC/GraphData/Canvas ownership, typed state distinction, zero-economics posture, and closed Dev boundary are source-backed and focused-testable. Current-guideline evaluator parity, protected Knowgrph integration, live rendering, Prod, and Cloudflare remain unclaimed. |
| Role-based Agent Team runtime | Runtime-ready in local Dev | The exact invocation tuple, collaboration Agent variants, built-in two-branch team, host-owned verifier/control/review owners, explicitly configured loopback model adapter, durable effect replay fence, private delegation, manager-owned synthesis, bounded accounting, and canonical stdio execution have focused proof. Real provider availability, remote Worker parity, Prod, and Cloudflare remain gated. |
| Repository packing contract | Runtime-ready for ACOS contract | The original invocation, schemas, bounds, output grammar, safety, independence, proof, and owner split are source-backed and focused-tested; combined local runtime readiness requires the exact protected Knowgrph tool proof and exact Agentic Canvas OS docs revision pin. |
| AI Voice Studio contract | Spec-complete | Canonical metadata routes, one wire identity, safety, typed request/result, bounds, clean-room policy, and deterministic ACOS validation are present. Runtime-ready promotion requires exact integrated Knowgrph revisions and passing local stdio MCP/runtime proof; live cloning, microphone behavior, provider quality, biometric deletion, Prod, and Cloudflare remain unverified. |
| Progressive Agents Dev runtime | Runtime-ready in Dev | The local facade is typed, bounded, source- and revision-fenced, tool-owner-preserving, specialist-owner-explicit, fail-closed, and no-copy. Default Worker execution, live tool use, and additional provider spend remain separately gated. |
| Agent Swarm Dev runtime | Runtime-ready offline in Dev | Native dynamic planning, session-owned durable atomic claims, horizontal recovery, stable idempotency, receipt-owner verification, cancellation, private intermediates, cost evidence, and base-agent-only synthesis pass focused proof. Default adapters, live provider execution, Prod, and Cloudflare remain gated. |
| Agent Toolkit runtime | Runtime-ready production contract | Native framework-neutral instrumentation, metadata-only trust-labelled durable traces and structured exports, percentile/bottleneck profiles, caller-declared digest binding, application authorization seams, per-principal durable admission, bounded trusted-evidence cohort comparison, deterministic quality/latency/cost recommendations, honest cost and evidence gaps, principal-derived isolation, and proposal-only learning pass combined proof. Remote telemetry remains untrusted and excluded; production requires durable bindings plus injected authorization/evaluation/telemetry, while measured improvement, Prod release, and Cloudflare deployment remain gated. |
| Sandbox Agents Dev runtime | Runtime-ready in Dev with bounded local Docker proof | The generic controller is joined by an immutable-image Docker adapter, deny-first application authorizer, atomic file checkpoint store, and separate 20-check verifier. The default Worker remains unconfigured; cloud hosting, environment bindings, online package egress, public ports, and formal certification remain gated. |
| Models and providers Dev runtime | Runtime-ready in Dev | The local registry is typed, bounded, revision-fenced, precedence-aware, feature- and transport-matched, secret-safe, fail-closed, and no-copy; adapter execution, provider capability truth, usage, and cost remain separately gated. |
| Mixture-of-agents docs contracts | Runtime-ready for docs | Contracts are route-complete, bounded, one-shot, no-copy, and cost-gated; live runtime remains separately gated. |
| Learning-loop docs contracts | Runtime-ready for docs | Contracts are route-complete, no-copy, bounded, and review-gated; live runtime remains separately gated. |
| Skill Evolution ACOS contract | Spec-complete; validating MCP client transport tested | The canonical contract is source-fenced, resumable, clean-room, epoch and mini-batch bounded, text-mutation-rate scheduled, validation-isolated, and review-only. Runtime-ready status requires exact integrated ACOS and Knowgrph revisions plus exact passing Knowgrph test commands, none of which this ACOS-only proof supplies. |
| Stateful orchestration docs contracts | Runtime-ready for docs | Contracts are route-complete, no-copy, checkpointed, bounded, and review-gated; live runtime remains separately gated. |
| Agentic video workflow Dev runtime | Runtime-ready in Dev | Native workflow schema, exact-span long-script corpus, expressive storyboard, scene rigs, temporal first-frame reference catalog/selection, optional strict entity/environment coverage, KGC/VLM/provider reference parity, action-beat blocking, background continuity, explicit rhythm, plot/dialogue retention, hierarchical planning, specialist negotiation, persistent checkpoint, completed-shot reuse, bounded retry, cost/ledger reuse, and MCP schema parity have focused proof; Prod mirror and Cloudflare remain gated. |
| Image to Three.js skill Dev runtime | Runtime-ready in Dev | Native PNG/JPG/JPEG textured-plane and SVG fill/stroke geometry projections have focused conversion, lifecycle, fallback, TypeScript, hygiene, and shared-surface browser proof; protected integration, Prod mirror, and Cloudflare remain gated. |
| Procedural image-to-3D quality contract | Runtime-ready within the Dev source-test boundary | Thirty-three focused selectors and the Canvas TypeScript, focused ESLint, hygiene, chunk-budget, and documentation-lint gates cover connected contour reconstruction, compact budgets, provenance, rigid action structure, the bounded loop clip, separated quality gates, and both export forms. Live provider and browser proof, deformable skinning, protected integration, Prod, and Cloudflare remain gated. |
| Long-horizon SuperAgent docs contracts | Runtime-ready for docs | Contracts are route-complete, sandbox-scoped, message-gated, artifact-backed, no-copy, bounded, and cost-gated; live runtime remains separately gated. |
| `knowgrph` local runtime capabilities | Gated by focused proof | Must be proven by the relevant `knowgrph` tests or local MCP calls. |
| Prod mirror | Human-authorized and post-verification only | Published only after the exact Candidate Manifest is authorized, deployed without rebuild, and represented by a successful Live Verification Receipt. |
| Cloudflare free-tier posture | Source-ready in Dev | Static assets bypass the Worker, root and version preview endpoints are disabled, CPU and anonymous admission are bounded, empty rooms remain ephemeral, unexpired reconnect tokens are reused, secret-safe custom observability is sampled, invocation logs and traces are off, Durable Object calls, writes, alarms, and retention are guarded, and dry-run bundle validation is required. Current quota consumption, globally exact admission, authenticated room creation, production hibernation behavior, and billing require separate provider or client evidence. |
| Cloudflare | Exact-candidate human authorization required | The Knowgrph reference controller consumes one authenticated Human Authorization Receipt, revalidates the complete dependency and policy closure, serializes by target plus candidate digest, deploys the already-built bytes, captures rollback evidence, and requires a Live Verification Receipt before mirror publication. |
| Integration authority continuity | Focused-tested in Dev | The provider-neutral integration owner injects ordinary claim continuation before validation, immediately before committing validated bytes, and before publication; it rejects projection drift, expired authority, changed repository bytes, claim replacement, and scope or write-set changes. This proof grants no protected merge, Production, or Cloudflare authority. |

## Revalidation

Run the focused commands in `VALIDATION-RUNBOOK.md` after any change to this folder. If a runtime owner in `$KNOWGRPH_ROOT` changes, add the relevant focused `knowgrph` test or MCP proof before updating this ledger.
