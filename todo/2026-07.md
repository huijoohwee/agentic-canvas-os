---
title: "Agentic Canvas OS Todo 2026-07"
doc_type: "Planning Ledger Shard"
schema: "todo-log/v1"
period: "2026-07"
scope: "cross-repository"
status: "append-only"
append_policy: "append-only"
date_heading_format: "YYYY-MM-DD"
source_contract: "../docs/TODO.md"
adoption_date: "2026-07-14"
historical_source: "$GITHUB_ROOT/knowgrph/todo-log.md"
historical_policy: "preserve-row-bytes"
---

# Todo 2026-07

This shard is governed by `../docs/TODO.md`. Pre-adoption rows retain their imported source bytes; new rows append at EOF.

## 2026-07-05

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| FloatingPanel Chat Storyboard response contract | Keep no-slash plain while routed invocations use KGC plus Agentic OS directive context | Split recognized leading route tokens from remaining requests before provider payload, prompt, KGC profile, fallback, and trace relevance. | `knowgrph/{canvas/src/features/chat,canvas/src/__tests__,docs/documents,todo-log.md}` | Chat Storyboard template contract | `chatRuntimeInvocationQuery`, `buildSubmitConversationMessages`, `buildAgenticOsRuntimeInvocationSystemPrompt` | No-slash prompt or `/prd-tad.create` prompt plus remaining query/media | Plain Markdown or `response:` YAML for no slash; structured KGC with PRD/TAD defaults while the model receives query/media only | Submitted text selects the response contract; route tokens are metadata and must not become product, named terms, request scope, provider user text, or backfilled answers. | Re-run Storyboard template contract, no-slash, PRD/TAD slash, TypeScript, hygiene, doc lint, and browser smoke when the chat surface is mounted. | 2026-07-08 |
| FloatingPanel Chat clean-slate fallback | Stop no-slash image trace output from becoming a backfilled MCP/Rich Media or computing-flow KGC | Classify active-stream/no-content provider traces as no-answer evidence, sanitize markdown image runtime URLs, and keep trace-only fallback on the plain base KGC contract. | `knowgrph/{canvas/src/features/chat,canvas/src/__tests__,docs/documents,todo-log.md}` | KGC request profile + response projection | `sanitizeRequestIntent`, `isTraceOnlyAssistantText`, `buildDeterministicBaseTemplateKgcTurn`, `normalizeKgcAssistantBodyForStorage` | No-slash `what ![image](runtime-url)` prompt plus provider stream trace | Query-responsive base KGC response with neutral `[attached image]`, no signed URL, no upload id, no synthetic MCP nodes, no computing-flow scaffold | Runtime media URLs are evidence pointers, not product identity or graph backfill authority; provider trace without content cannot seed answer surfaces or computing-flow templates. | Re-run clean-slate trace regression, chat response contract tests, TypeScript, hygiene, and a live no-slash image chat smoke before deploy approval. | 2026-07-08 |
| FloatingPanel Chat slash response variants | Make `/` select chatResponseBaseContract variants only when invoked | Parse the submitted user message for `/storybuilding`, `/research-agent`, `/video-agent`, or `/care-agent`; otherwise use the plain base contract. | `knowgrph/{canvas/src/features/chat,canvas/src/__tests__,docs/documents,todo-log.md}` | Chat skill registry + submit request context | `parseChatSkillSlashInvocation`, `buildChatSkillInvocationSystemPrompt`, `buildChatSubmitRequestContext` | Leading slash command plus remaining query | Base contract with narrow variant prompt or plain vanilla contract | Submitted text is the SSOT; no selected-skill submit alias or stale skill id can activate a variant. | Re-run focused chat response contract tests, composer command tests, TypeScript, hygiene, and one live slash chat smoke before deploy approval. | 2026-07-08 |
| Probe-tree care-agent runtime | Make branching clarification runtime-ready without deploy | Route care-agent clarification through local probe-tree MCP tools, host-owned Ollama env, markdown checkpoint nodes, and memory exemplars; keep sibling demo Dev-only. | `knowgrph/{mcp,canvas/src/features/agent-ready,docs/documents,todo-log.md}; huijoohwee/docs/knowgrph-care-agent-demo.md` | Probe-tree MCP + care-agent demo doc | `generateProbeOptionsWithLocalModel`, `generateProbeOptions`, `selectProbeOption`, `evolveProbeTree` | Redacted care context, recalled exemplars, optional local model env, selected option | Typed probe options, `type: probe` node, `branches-to` edge, memory exemplar, care-agent proof fields | Model adapter is optional and host-owned; generate never mutates graph; markdown remains checkpoint SSOT; deploy guards stay explicit. | Re-run probe-tree MCP tests, care-agent runtime-ready test, TypeScript, hygiene, and sibling dynamic panel parse after future probe changes. | 2026-07-07 |
| FloatingPanel Chat invocation discovery | Make `/`, `@`, and `#` runtime invocations discoverable in the controlled composer | Reuse shared menu, chat, variable, media, keyword, memory, thumbnail, and vdeoxpln owners; inject exact tools and fail-closed scope rules. | `knowgrph/{canvas/src/{features/chat,features/agent-ready,lib/{command-menu,markdown-core},components/ui,__tests__},docs/documents,todo-log.md}` | Chat composer invocation adapter | `resolveChatComposerTrigger`, `useUploadedMediaInlineCommandCandidates`, `buildChatInvocationSystemPrompt`, `buildKnowgrphVdeoxplnRoutingPlan` | Caret context, registries, active variables, cached FloatingPanel Media inventory, provider/model | Commands, `{{key}}` media-variable chips, media embeds with mini thumbnails, scoped MCP/agent/model/media directives | Registries remain SSOT; card and Chat share one media projection, thumbnail renderer, and storage request; unavailable tools produce handoffs, not claimed execution. | Re-run composer/runtime tests, TypeScript, hygiene, doc lint, and local `@` media plus `#media` browser proof. | 2026-07-06 |
| FloatingPanel Chat pipeline visibility | Make workspace ingestion, parsing, and rendering state inspectable without a second runtime | Derive a clickable three-stage status strip from Source File lifecycle and graph state; key workspace context by exact cached text hashes. | `knowgrph/{canvas/src/features/chat,canvas/src/__tests__,docs/documents,todo-log.md}` | Chat surface state + bounded text hash cache | `createFloatingPanelChatPipelineStages`, `buildFloatingPanelChatSourceFilesSignature` | Active sources, document text, graph state | Semantic pipeline actions and stale-safe context cache identity | Existing Source Files and GraphData remain SSOT; chat only projects state and seeds prompts. | Re-run focused chat pipeline/cache tests, TypeScript, doc lint, hygiene, and local browser proof after future lifecycle changes. | 2026-07-06 |
| XR Mode ingest/render performance | Keep Import URL, Import local files, and XR Mode fast for `.gltf`, `.glb`, `.ply`, and `.spz` | Cache GLB/GLTF payloads, probe URL MIME/name hints, range-preview local/URL PLY rows, gate full promotion after preview paint, progressively upload full attributes, and keep validation assets env-only. | `knowgrph/{canvas/src/{features/markdown-workspace/workspaceImport,features/three,lib/assets,__tests__,tests/runners},docs/documents,todo-log.md}` | XR import/runtime cache pipeline | `resolveSpatialCaptureUrlContentForImport`, `loadModelAssetRenderPayload`, `loadSpatialCapturePointCloudPreview`, `loadSpatialCapturePointCloud`, `buildGaussianSplatGeometry`, `updateGaussianSplatGeometrySort` | URL/local model or spatial-capture source plus optional operator-supplied validation file | XR-ready manifest, direct loader input, bounded payload/source cache, preview-ready first paint, full-fidelity promotion, deferred splat resort | Keep model and spatial-capture import source-owned; Markdown stores manifests and cache keys, not payload bytes; parser preserves validation-size source arrays while the renderer grows `instanceCount` and attribute views after range-backed first paint. | Re-run focused `workspace.import.xrSpatialCapture.*`, model payload cache, hardcode scan, TypeScript, hygiene, live Import URL fidelity check, and env-only external PLY parse before future XR ingest/render changes. | 2026-07-06 |
| AI Singapore SEA-LION MCP Sidecar | Expose SEA-LION sidecar tools through MainPanel MCP and local MCP without deploy | Add `sealionMcp.*` KTV rows and local MCP bridge tools for `detect_language_variant`, `translate_localize`, and `safety_check`; keep `KNOWGRPH_MCP_SEALION_API_KEY` server-owned and no browser persistence. | `knowgrph/{mcp/{sealion-sidecar-runtime.js,local-tool-contract.js,server.js},canvas/src/{features/panels/views,features/agent-ready,__tests__},docs/documents,todo-log.md}` | MainPanel MCP + local stdio MCP | `callSealionSidecarTool`, `buildMcpDocEntries`, `buildKnowgrphLocalMcpToolDefinitions` | `sealionMcp.*`, SEA-LION sidecar MCP Streamable HTTP URL, server-side env key | Searchable MCP rows, local `sealion.*` tools, hosted sidecar forwarding | Treat SEA-LION as regional specialist sidecar; forward through hosted sidecar with server auth; no copied prompt internals, browser keys, publish patches, or Cloudflare deploy. | Mirror AgenticRAG schema and publish repo only after explicit Prod/Cloudflare approval. | 2026-07-05 |
| AI Singapore SEA-LION MainPanel Integration | Add SEA-LION as a provider-neutral regional chat/specialist sidecar without deploy | Register `sealion` in shared chat/provider owners, MainPanel Integrations KTV rows, Dev proxy allowlist, and provider-family normalization; keep `KNOWGRPH_CHAT_PROXY_SEALION_API_KEY` server-managed and BYOK RAM-only. | `knowgrph/{canvas/src/{lib,features/panels/views,features/storyboard-widget-manager,__tests__},canvas/vite.config.ts,docs/documents,todo-log.md}` | Shared chat provider + Integrations rows | `normalizeChatProviderId`, `resolveIntegrationEntryMeta`, `normalizeTextGenerationWidgetPropertiesForProviderFamily` | `sealionApi.*`, official inferencing API doc, hosted model ids, `/v1/models`, `/v1/chat/completions`, `/v1/embeddings` | Searchable MainPanel SEA-LION config, proxy headers, provider inference | Treat SEA-LION as optional OpenAI-compatible provider and MCP sidecar; follow `https://docs.sea-lion.ai/guides/inferencing/api.md`; no renderer/parser forks, source keys, fixtures, publish patches, or deployed changes. | Add publish-repo proxy parity and AgenticRAG schema mirror only after explicit Prod/Cloudflare approval. | 2026-07-06 |

## 2026-07-09

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Agentic Canvas OS command grammar + MCP readiness validation | Confirm architecture readiness and headless UI approach for integrating Agentic Canvas OS commands (/, @, #). | Validate MCP Stdio/Cloudflare HTTP readiness and confirm headless architecture where knowgrph acts as the intelligence plane without mutating files. | `knowgrph/{mcp/agentic-canvas-os-docs-contract.mjs,cloudflare/workers/knowgrph-mcp/package.json,todo-log.md}` | MCP tool definitions | `AGENTIC_CANVAS_OS_DOCS_TOOL_DEFINITION` | User query on MCP readiness for lovable, vercel, cmdk | Confirmed MCP availability and SSOT grammar resolution without mutating files | Adhere to min-viable-max-value and token economics by relying on MCP SSOT rather than front-end specific cmdk npm packages. | Update cross-repo docs mapping for MCP and agentic canvas OS grammar. | 2026-07-09 |

## 2026-07-10

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Cloudflare AI Gateway high-ROI execution queue | Turn existing gateway readiness into a small, proofable operator plan that lowers spend and clarifies the current Cloudflare surface boundary. | Publish one canonical AI Gateway enhancement plan that prioritizes deterministic cache policy, intent-mapped dynamic routes, metadata-scoped spend limits, analytics proof loops, and future migration away from legacy universal-endpoint wording without touching runtime code. | `knowgrph/{README.md,docs/documents/{knowgrph-ai-gateway-enhancement-plan.md,knowgrph-cloudflare-document.md},todo-log.md}` | AI Gateway optimization roadmap | Doc-owned execution queue | Existing gateway-ready architecture, Cloudflare AI Gateway docs, and current README/Cloudflare baseline | Canonical plan plus README and Cloudflare pointers for the next runtime slice | Since the gateway boundary already exists, the highest-ROI no-code move is to operationalize caching, routing, budgets, and proof while explicitly naming the modern REST-surface migration path. | When code edits open, start with metadata plus cache headers and one low-risk dynamic route because that gives the fastest measurable token-cost proof with minimal runtime churn. | 2026-07-11 |
| Publish-doc handoff to shared promotion recovery card | Let public docs point operators to the shared browser-published recovery surface instead of copying recovery semantics into another doc-only branch. | Update source MCP service owner docs plus the publish-side MCP service notices, onboarding index, and install-boundary release note so they point to the shared MCP Apps agent-ready card for template-only `#promotion.retry <path...>` semantics and keep exact retry paths browser-local. | `knowgrph/{docs/documents/knowgrph-mcp/{knowgrph-mcp-service-prd-tad.md,knowgrph-mcp-service-prd-tad.companion.md},todo-log.md},huijoohwee.github.io/docs/documents/{knowgrph-mcp-service-prd-tad.md,knowgrph-mcp-service-prd-tad.companion.md,knowgrph-mcp-onboarding-index.md,knowgrph-mcp-install-boundary-release-note-20260710.md}` | Publish docs + MCP Apps recovery surface | Shared-source publish handoff | Existing published `promotionRecovery` MCP Apps contract and prior onboarding/install docs | Public docs that route recovery readers to the shared browser-published MCP Apps card instead of inventing a parallel recovery write-up | After the shared MCP Apps card exists, the highest-ROI publish follow-up is to make the docs hub and release-note entry points point at that source owner so public readers land on one recovery contract path. | Next, consider adding a single source-owned docs reference inside the repo README only if it improves first-read operator recovery flow without crowding the current install-first opening. | 2026-07-11 |
| Published promotion recovery operator card parity | Mirror the retry contract into an existing browser-published operator surface with a named source owner and parity test. | Expose a template-only `promotionRecovery` operator contract through the shared MCP Apps readiness payload and static HTML card so published surfaces advertise `#promotion.retry <path...>` semantics without pretending to know failure-specific paths. | `knowgrph/{canvas/src/features/agent-ready/mcpAppsReadyContract.mjs,canvas/src/__tests__/agentReadyHttpMcpParity.test.ts,todo-log.md}` | MCP Apps agent-ready surface | Published operator recovery card | Existing documented promotion retry contract plus browser-local runtime inspection contract | Shared readiness payload and browser-published MCP Apps HTML that surface the retry command template, scope, boundaries, and exact-command source with parity coverage | After browser-local inspection exposes the real runnable retry command, the next highest-ROI move is to mirror the same contract into a published operator surface so first-read external operators can understand recovery semantics without opening source docs. | Next, consider mirroring the same contract into any publish-side docs hub or release-note widget only if it reuses this shared MCP Apps source owner rather than introducing a parallel copy path. | 2026-07-11 |
| Browser-local promotion recovery inspection parity | Turn the documented retry contract into a real operator-facing browser-local inspection surface with a stable machine-readable shape. | Derive a compact `promotionRecovery` contract from finalize failure fields inside the local chat pipeline inspector, expose the runnable retry command and recovery semantics through WebMCP/browser-local inspection, and pin both failure and idle cases with focused tests. | `knowgrph/{canvas/src/features/agent-ready/{localChatPipelineStateInspection.ts,knowgrphAgentReadyToolContract.mjs},canvas/src/__tests__/{chatResponseContractPrompt.test.ts,webMcpRuntime.test.ts},todo-log.md}` | Browser-local chat pipeline inspection | Operator-facing recovery inspection contract | Existing finalize recovery fields, browser-local chat pipeline tool, web-MCP runtime, and focused retry tests | A browser-local/WebMCP inspection surface that exposes the exact runnable retry command plus mirror-only recovery semantics without forcing callers to parse ledger prose | Once the retry contract is documented, the next highest-ROI step is to expose that same contract in the operator inspection surface already used by browser-local and WebMCP tooling so recovery is actionable in-context. | Next, mirror the same `promotionRecovery` contract into any future browser-published operator dashboard card only after that UI surface has a named source owner and parity test. | 2026-07-11 |
| Promotion retry operator contract | Make artifact mirroring failures recoverable from the exact command already surfaced by finalize instead of relying on source-code discovery. | Publish one canonical companion that defines when `#promotion.retry <path...>` is valid, where it must appear, what it reuses, and which shared append-focus/chat boundaries it must keep honoring. | `knowgrph/{docs/documents/{knowgrph-chat-ai-markdown-pipeline-document.md,knowgrph-chat-ai-markdown-pipeline-document.promotion-retry.md},todo-log.md}` | Chat pipeline promotion recovery | Operator contract documentation | Existing finalize retry command, browser-local finalize snapshot fields, warning toast, and focused retry tests | One source-owned recovery contract that names the exact command shape, operator surfaces, retry semantics, and composer guardrails without touching runtime code | The highest-ROI follow-up after install/onboarding cleanup is to make promotion failure recovery explicit because it shortens operator recovery time and protects the local-first contract without requiring new runtime breadth. | Next, surface the same retry contract from any future browser-published operator inspection page only after that surface has a named source owner and parity check. | 2026-07-11 |
| Publish-side grammar-first MCP onboarding parity | Keep public first-read docs aligned with the install-first, control-plane-executes-grammar contract after the browser-published onboarding update. | Mirror the fastest grammar path, `knowgrph.agentic_canvas_os.docs.invoke` ownership, and live grammar examples into the published onboarding index, install-boundary release note, and published tech-stack landing note. | `knowgrph/{todo-log.md},huijoohwee.github.io/docs/documents/{knowgrph-mcp-onboarding-index.md,knowgrph-mcp-install-boundary-release-note-20260710.md,knowgrph-tech-stack-document.md}` | Publish-facing MCP grammar path | Documentation mirror alignment | Source-side grammar-first onboarding contract plus existing publish-side install docs | Public docs that make install location, grammar execution boundary, and live examples explicit without requiring readers to infer them from source-only docs | After source docs and browser-published onboarding are aligned, the next highest-ROI move is to remove the same endpoint inference from public docs so external readers hit the right live grammar path faster. | Next, mirror the same grammar-first operator path into any future publish-side docs hub or setup widget only after that surface has a named source owner and parity check. | 2026-07-11 |
| Agentic Canvas OS grammar-first onboarding | Make external operators understand exactly how live `/`, `#`, and `@` invocation works without inferring it from broader MCP prose. | Add one compact remote grammar invocation contract to the canonical agent-ready and MCP overview docs; keep `/knowgrph/mcp` install-first and reserve live grammar resolution for `knowgrph.agentic_canvas_os.docs.invoke` on `/knowgrph/control-plane/mcp`. | `knowgrph/{docs/documents/knowgrph-agent-ready-document.md,docs/documents/knowgrph-mcp/knowgrph-mcp.md,todo-log.md}` | Operator-facing grammar contract | Remote grammar invocation contract + fastest grammar path | Startup-priority grammar recommendation plus existing dual-surface MCP boundary | Source-owned docs that tell operators where grammar is installed, where it executes, and which tool resolves it live | The highest-ROI doc-only implementation is to remove endpoint inference at the source, because grammar differentiation is valuable only when operators can see the exact remote call path immediately. | Next, mirror the same grammar-first operator path into any future browser-published setup widget or public docs hub only after that surface has a named source owner and parity check. | 2026-07-11 |
| Artifact promotion failure recovery | Make failed GitHub or storage mirroring immediately retryable from the chat thread, browser-local inspection, shared warning toast, one-click composer insertion, same-toast success confirmation, and shared caret-at-end placement for retry, quick-action, and queued handoff prompt insertions. | Surface an exact runnable `#promotion.retry <path...>` command in the finalize ledger, local finalize snapshot, deduped toast, shared toast action, post-click confirmation, and shared append-focus composer path whenever local artifacts already exist and promotion fails; reuse that same append-focus path for quick-action prompt seeding, surface-model append prompts, and queued live-canvas hero handoff too, normalize pure invocation-query spacing in the shared open-seed helper for both append-event and queued-handoff delivery while preserving raw seed text for path-bearing or trailing-space commands, let live-canvas hero handoff delegate that normalization to the shared owner instead of pre-normalizing locally, centralize open-only chat entrypoints behind the same shared owner for toolbar and settings surfaces, resolve append-event and queued-handoff seeds through one canonical shared seed object before dispatch or queue storage, add focused open-only and queued-handoff success regressions, and enforce the shared event boundary through registry-addressable canvas action event tests. | `knowgrph/{canvas/src/features/{chat,agent-ready,canvas},canvas/src/lib/ui,canvas/src/__tests__,canvas/src/tests/registry,todo-log.md}` | Chat finalize artifact ledger + local inspection snapshot + shared toast + shared ui action runtime + shared append-focus composer path | `buildWorkspacePromotionRetryCommand`, `buildWorkspacePromotionRetryToast`, `buildChatPromotionRetryInsertAction`, `runChatPromotionRetryUiAction`, `retryGeneratedChatWorkspaceArtifactPromotion`, `publishLocalChatPipelineFinalizeSnapshot`, `useFloatingPanelChatSurfaceModel`, `openFloatingPanelChat`, `openFloatingPanelChatWithSeed`, `resolveFloatingPanelChatSeed`, `handoffLiveCanvasHeroQuery`, `flushFloatingPanelChatInputHandoff`, `applyFloatingPanelChatInputAppend` | Failed promotion result with one or more saved workspace artifact paths, plus standard quick-action prompt insertions, surface-model append prompts, queued live-canvas hero handoff drafts, and open-only chat entrypoints from toolbar/settings | Finalize response, retry helper, browser-local chat pipeline inspection, shared warning toast, toast action, same-toast success confirmation, retry-command composer placement, quick-action prompt insertion, surface-model append prompt dispatch, normalized pure-token append-event and queued-handoff seed text, preserved raw path-bearing and trailing-space commands, hero handoff delegation through the shared open-seed owner, open-only chat entrypoints through the shared open helper, one canonical resolved seed object for both delivery modes, queued handoff draft flush, open-only helper regression coverage, queued-handoff success regression coverage, and event-boundary regression coverage all reuse the same exact append-and-caret or open-only source owners | Retry guidance, quick-action prompt seeding, surface-model append prompts, queued handoff drafts, and open-only chat entrypoints should reuse already-saved local artifact paths or owned prompt strings instead of introducing a second composer-control channel or duplicated floating-panel open emitters, and both delivery modes should apply the same pure-token normalization before dispatch without mutating path arguments or raw command suffix spacing so focused CI can catch regressions immediately. | Next, route any future relay-owned prompt seeding through the same shared open-seed helper or append-event boundary, and avoid new direct `setInput` insertion branches. | 2026-07-11 |
| Mobile-first enhancement plan | Turn the current mobile-first recommendation into a source-owned execution queue without touching runtime code. | Publish one canonical upstream plan that prioritizes grammar reachability, heavy-runtime gating, virtual-keyboard proof, route-action evidence, and Dev -> Prod -> Cloudflare responsive parity. | `knowgrph/{docs/documents/{knowgrph-mobile-first-enhancement-plan.md,knowgrph-ui-ux-design-document.md},todo-log.md},huijoohwee.github.io/schema/AgenticRAG/README.md` | Mobile-first docs and schema mirror alignment | Canonical plan + discoverability note | Existing mobile-first goal, UI/UX contract, feature-map evidence, and no-code constraint | One source-owned implementation plan plus schema-mirror note for the next mobile-first execution slice | Under a no-code boundary, the highest-ROI implementation is to convert recommendation into one canonical execution queue that future agents can ship from without re-deciding scope, proof, or release discipline. | When code edits are allowed, start with the grammar quick bar and virtual-keyboard proof because they improve mobile time-to-value fastest without broad runtime churn. | 2026-07-10 |
| Machine-readable and browser-published onboarding alignment | Keep the shortest install path, control-plane boundary, and offline proof path consistent across public machine-readable and UI entry points. | Update the published `llms.txt` onboarding copy, add a compact Fastest Path block to the source-owned MCP Apps agent-ready surface, and expose the same sequence through structured readiness data. | `huijoohwee/content/knowgrph/llms.txt,knowgrph/canvas/src/features/agent-ready/mcpAppsReadyContract.mjs,knowgrph/canvas/src/__tests__/agentReadyHttpMcpParity.test.ts,knowgrph/todo-log.md` | MCP onboarding entry points | Content + source-owned UI alignment | Startup-priority install order and offline harness guidance | Public machine-readable and browser-published onboarding surfaces that direct operators to `/knowgrph/mcp` first, reserve control plane for session-capable grammar flows, and point evaluation to the source-side harness path | After doc-only alignment, the highest-ROI next move is to align the shortest machine-readable and browser-published entry points so external hosts, crawlers, and operators receive the same low-friction install story without reading long-form docs first. | Next, align any future setup widget or public docs hub card to the same sequence only after that surface has a named source owner and a parity check. | 2026-07-10 |
| Publish-surface startup-priority alignment | Keep the public docs mirror aligned with the new installability-first onboarding story without changing runtime code. | Mirror the fastest install path, control-plane separation, and offline harness proof-path guidance into the published install contract, onboarding index, release note, and tech-stack landing note. | `knowgrph/todo-log.md,huijoohwee.github.io/docs/documents/{knowgrph-mcp-install-contract.md,knowgrph-mcp-onboarding-index.md,knowgrph-mcp-install-boundary-release-note-20260710.md,knowgrph-tech-stack-document.md}` | Publish-facing MCP onboarding docs | Documentation mirror update | Startup-priority roadmap plus updated source-side install, agent-ready, and harness docs | Public entry points that match the source-side installability-first story and point evaluators to the cheapest proof path before hosted complexity | The highest-ROI public follow-up after source-doc alignment is to keep first-read publish docs coherent with the same endpoint boundary and offline proof guidance, so external operators reach the right install path faster and incur less setup cost. | Next, mirror the same installability-first ordering into any future published setup widget, docs hub, or release landing page only after that surface has a named source owner and a doc-truth audit. | 2026-07-10 |
| Startup-priority alignment for install and agent-ready docs | Align operator-facing MCP onboarding and agent-ready narrative with the startup priority order while staying doc-only. | Tighten the install contract with a fastest decision path and host matrix, update the agent-ready document to lead with installability and grammar differentiation, and surface the local harness as the offline evaluation path. | `knowgrph/{docs/documents/{knowgrph-mcp-install-contract.md,knowgrph-agent-ready-document.md,knowgrph-superagent-harness.md},todo-log.md}` | Operator onboarding + agent-ready doc alignment | Canonical doc update | Startup-priority roadmap plus existing install, agent-ready, and harness docs | Sharper source-owned onboarding path that favors time-to-value, proof density, and offline evaluation before hosted complexity | The highest-ROI follow-up after publishing the roadmap is to make the first-read install and agent-ready docs reflect that same operating order so new operators reach a working evaluation path with less ambiguity and lower setup cost. | Next, mirror this same installability-first order into any future release note, setup widget, or published tech-stack landing doc only after those surfaces are source-owned and audited. | 2026-07-10 |
| Startup-priority doc-only execution queue | Turn the README-derived startup recommendations into tracked, source-owned execution guidance without touching runtime code. | Publish a canonical next-step priorities doc that ranks install boundary, Agentic Canvas OS grammar positioning, readiness proof, offline harness adoption, and Dev -> Prod -> Cloudflare discipline, then surface it from the root README. | `knowgrph/{README.md,docs/documents/knowgrph-next-step-priorities.md,todo-log.md}` | Startup roadmap + README feature-doc routing | Canonical doc update | Current README positioning, MCP install docs, harness docs, and Agentic Canvas OS docs boundary | One source-owned priority roadmap plus root README discoverability for the current no-code phase | The highest-ROI implementation under a no-code constraint is to convert strategy into one canonical roadmap that points contributors and future agents toward installability, differentiation, proof, offline evaluation, and release discipline before any new feature breadth. | Next, keep install-facing and agent-ready docs aligned with this priority order and only expand deeper feature docs when they improve endpoint clarity, grammar differentiation, proof density, offline demoability, or release discipline. | 2026-07-10 |
| MainPanel readiness contract cleanup | Make MainPanel Integrations and MCP readiness claims implementation-accurate without code edits. | Label MainPanel claims as `documented`, `browser-published`, or `runtime-executable`; treat external `knowgrph.tool.*` bridge ids as planned until registry owners ship them; anchor provider coverage to the full Settings-backed universe. | `knowgrph/{README.md,mcp/README.md,docs/documents/knowgrph-mainpanel-readiness-rubric.md,todo-log.md}` | MainPanel readiness rubric + release gate | Canonical doc updates | Audit findings for MainPanel Integrations and MCP | Source-owned readiness rubric, README/MCP wording cleanup, and deploy-gate checklist | Fix readiness drift at the documentation contract first because it removes false-positive claims immediately, lowers operator ambiguity, and avoids spending time or tokens on unshipped runtime paths. | Mirror the same readiness labels into any future Prod or Cloudflare release notes only after runtime validation for the promoted surface exists. | 2026-07-10 |
| MainPanel readiness docs propagation | Extend the readiness rubric into the larger canonical docs set without touching runtime or test code. | Relabel MainPanel wording in agent-ready, API, and MCP overview docs so UI shells are not mistaken for executable runtime owners and setup rows are not described as live capabilities. | `knowgrph/{docs/documents/knowgrph-agent-ready-document.md,docs/documents/knowgrph-api-document.md,docs/documents/knowgrph-mcp/knowgrph-mcp.md,todo-log.md}` | Canonical readiness wording propagation | Documentation relabel pass | New readiness rubric plus existing MainPanel audit findings | Wider canonical doc alignment with explicit `documented` / `browser-published` / `runtime-executable` language | Source-owned doc propagation yields immediate contract consistency with zero engine churn and keeps later test/runtime cleanup scoped to the remaining real drift. | When code edits are allowed, audit `canvas/src/__tests__/mainPanelMcpExternalToolServers.test.tsx` and `canvas/src/__tests__/helpers/mainPanelMcpExpectations.ts` for remaining doc-vs-runtime drift. | 2026-07-10 |
| MainPanel external MCP harness drift cleanup | Neutralize false-positive executable-readiness signals in the focused MainPanel MCP test owners. | Keep deferred external bridge ids asserted as `documented`/pattern-only contract text with deferred schema access; forbid executable-runtime wording in the focused external MCP harness checks. | `knowgrph/{canvas/src/__tests__/mainPanelMcpExternalToolServers.test.tsx,canvas/src/__tests__/helpers/mainPanelMcpExpectations.ts,todo-log.md}` | Focused MainPanel MCP harness expectations | `assertDeferredExternalMcpBridgeIdsStayDocumentedContract`, external MCP SSOT checks | External MCP bridge-id rows and rendered MCP hub text | Focused harness proof that deferred bridge ids stay non-secret, deferred, and non-executable in test expectations | The highest-ROI source fix after doc relabeling is the harness boundary because it prevents tests from quietly preserving a stronger runtime claim than the docs and owners support. | Next, audit provider-coverage expectations so the full Settings-backed universe remains canonical and any SuperAgent/demo subset stays explicitly scoped. | 2026-07-10 |
| MCP install contract for third-party hosts | Make public install, control-plane grammar invocation, and host recipes explicit without changing runtime code. | Publish one source-owned install contract that tells hosts to install `/knowgrph/mcp`, add `/knowgrph/control-plane/mcp` only for session-capable grammar invocation, and mirror the same wording into the publish-docs tree. | `knowgrph/{README.md,docs/documents/knowgrph-mcp-install-contract.md,todo-log.md},huijoohwee.github.io/docs/documents/knowgrph-mcp-install-contract.md` | Remote MCP install boundary | Canonical doc + mirror update | Public discovery endpoint, control-plane endpoint, host setup recipes | One install truth for ChatGPT, Claude, Vercel, Lovable, and generic MCP clients | A single install contract removes endpoint ambiguity at the source, preserves the read-only vs orchestration split, and gives app builders a fail-closed path when dual-surface MCP session support is unavailable. | Next, add the new install-contract link to any future agent-ready landing pages or setup widgets that surface MCP onboarding metadata. | 2026-07-10 |
| MCP install contract surfacing | Put the new install boundary in the first operator-facing docs people read before choosing an endpoint. | Link the install contract from the canonical agent-ready landing doc and add the same boundary note to the published tech-stack overview so remote operators see `/knowgrph/mcp` vs `/knowgrph/control-plane/mcp` before setup drift starts. | `knowgrph/{docs/documents/knowgrph-agent-ready-document.md,todo-log.md},huijoohwee.github.io/docs/documents/knowgrph-tech-stack-document.md` | Agent-ready landing copy + publish overview | Documentation cross-link pass | New MCP install contract plus current operator-facing docs | Earlier visibility of the correct MCP setup boundary in both source and publish surfaces | Surfacing the contract from the first-read docs is the highest-ROI follow-up because it reduces wrong-endpoint installs before users ever reach deeper MCP reference material. | Next, mirror the same install-contract link into any future browser-published setup widget or agent-ready landing UI once code edits are allowed. | 2026-07-10 |
| MCP overview onboarding link | Make the dedicated MCP overview point directly to the canonical setup contract instead of making operators infer the next doc. | Add an explicit install-contract pointer inside `knowgrph-mcp.md` so the MCP overview itself routes setup readers to one canonical onboarding answer before they compare public discovery vs control-plane details. | `knowgrph/{docs/documents/knowgrph-mcp/knowgrph-mcp.md,todo-log.md}` | MCP overview onboarding note | Documentation cross-link pass | Existing MCP overview plus canonical install contract | MCP reference page that self-routes readers to the correct setup contract | This is the next highest-ROI doc follow-up because MCP-specific readers often land in the overview first; linking the install contract there closes the remaining doc-only onboarding gap without touching runtime code. | Next, expose the same canonical install-contract pointer in any browser-published MCP setup surface when code edits are allowed. | 2026-07-10 |
| MCP install boundary release note | Make the boundary change visible in recent-update scans without requiring readers to open the full install contract first. | Publish a short release note that announces `/knowgrph/mcp` as the install/discovery URL, `/knowgrph/control-plane/mcp` as the grammar/orchestration surface, and the install contract as the canonical onboarding source; mirror it into publish docs. | `knowgrph/{docs/documents/knowgrph-mcp-install-boundary-release-note-20260710.md,todo-log.md},huijoohwee.github.io/docs/documents/knowgrph-mcp-install-boundary-release-note-20260710.md` | MCP install-boundary change log | Documentation release note | Existing install contract, README/agent-ready/MCP overview routing, publish-docs mirror | One short recent-change artifact for operators scanning release history | A release note is the highest-ROI remaining doc-only follow-up because it makes the new boundary discoverable for readers who track change logs rather than architecture docs, without implying any runtime mutation or deploy event. | Next, add this release-note link to any future docs index or changelog landing page once that index has a named source owner. | 2026-07-10 |
| MCP onboarding index | Make the onboarding trail one click deep instead of requiring readers to discover individual docs organically. | Publish a small MCP onboarding index that links the install contract, install-boundary release note, agent-ready doc, and MCP overview, then mirror it into publish docs. | `knowgrph/{docs/documents/knowgrph-mcp-onboarding-index.md,todo-log.md},huijoohwee.github.io/docs/documents/knowgrph-mcp-onboarding-index.md` | MCP onboarding landing page | Documentation index | Existing MCP onboarding docs set and endpoint boundary | One small landing page for remote MCP onboarding | This is the highest-ROI remaining doc-only follow-up because it turns the scattered onboarding trail into a single landing page without inventing new runtime claims or touching product code. | Next, add a link to this onboarding index from any future docs table-of-contents or changelog hub once those surfaces have named source owners. | 2026-07-10 |
| MCP onboarding index backlink surfacing | Make the new onboarding index discoverable from the install-facing docs people already open. | Add short backlinks from README, the install contract, the install-boundary release note, and the published tech-stack overview to `knowgrph-mcp-onboarding-index.md` so the onboarding path works in both directions. | `knowgrph/{README.md,docs/documents/{knowgrph-mcp-install-contract.md,knowgrph-mcp-install-boundary-release-note-20260710.md},todo-log.md},huijoohwee.github.io/docs/documents/knowgrph-tech-stack-document.md` | MCP onboarding backlink pass | Documentation cross-link pass | New onboarding index plus existing install-facing docs | Bidirectional onboarding trail across source and publish docs | This is the highest-ROI remaining doc-only follow-up because a hub without backlinks still depends on luck; surfacing it from the already-touched install-facing docs makes the onboarding trail materially easier to find without creating new runtime claims. | Next, add the same onboarding-index pointer to any future docs hub, changelog hub, or browser-published setup surface once those owners are explicitly named. | 2026-07-10 |
| MCP publish-doc backlink parity | Keep the mirrored publish docs as easy to navigate as the source docs for MCP onboarding. | Add the same onboarding-index backlink to the published install contract and published install-boundary release note so the mirrored docs preserve the same guided path as the source repo. | `knowgrph/todo-log.md,huijoohwee.github.io/docs/documents/{knowgrph-mcp-install-contract.md,knowgrph-mcp-install-boundary-release-note-20260710.md}` | Publish-doc MCP backlink parity | Documentation cross-link pass | Existing publish MCP docs plus onboarding index | Publish-side guided onboarding parity with source docs | This is the highest-ROI remaining doc-only follow-up because publish readers should not lose the guided path that already exists upstream; mirroring the same backlinks keeps source and publish onboarding behavior aligned. | Next, add the onboarding-index pointer to any future publish-side docs hub or table-of-contents page once that surface has a named source owner. | 2026-07-10 |
| MCP source-doc backlink completion | Make the source-side agent-ready and MCP overview docs offer the same shortest guided path as the other install-facing docs. | Add the onboarding-index pointer to `knowgrph-agent-ready-document.md` and `knowgrph-mcp.md` so both docs surface the one-page onboarding route instead of only the install contract. | `knowgrph/{docs/documents/knowgrph-agent-ready-document.md,docs/documents/knowgrph-mcp/knowgrph-mcp.md,todo-log.md}` | Source-doc MCP backlink completion | Documentation cross-link pass | Existing source MCP docs plus onboarding index | Source-side shortest-path parity across install-facing docs | This is the highest-ROI remaining doc-only follow-up because the agent-ready and MCP overview docs are common entry points; adding the onboarding-index pointer there completes the source-side guided path without creating new runtime claims. | Next, add the onboarding-index pointer to any future docs hub or browser-published setup surface once those owners are explicitly named. | 2026-07-10 |
| MCP superseded-doc redirect note | Prevent historical publish-side MCP docs from sending readers down stale runtime/setup paths. | Add a short current-onboarding redirect note to the superseded publish-side `knowgrph-mcp-agentic-canvas-os-prd-tad.md` so readers are sent to the onboarding index and install contract before using old topology details. | `knowgrph/todo-log.md,huijoohwee.github.io/docs/documents/knowgrph-mcp-agentic-canvas-os-prd-tad.md` | Superseded publish-doc redirect note | Documentation redirect copy | Historical publish-side MCP PRD/TAD plus current onboarding docs | Safer historical-doc entry path for remote MCP readers | This is the highest-ROI remaining doc-only follow-up because superseded docs still attract readers; adding an explicit redirect to the current onboarding docs reduces stale endpoint or topology interpretation without touching runtime behavior. | Next, add the same redirect note to any other superseded MCP-facing publish docs that still receive inbound references once those documents are audited. | 2026-07-10 |
| MCP publish-doc entry-point audit follow-up | Redirect realistic publish-side MCP readers from adjacent Agentic OS docs into the current onboarding path. | Add a short remote-MCP onboarding pointer to the mirrored `knowgrph-agentic-os-follow-on-prd-tad.md` so readers landing there are sent to the onboarding index and install contract instead of inferring the endpoint boundary indirectly. | `knowgrph/todo-log.md,huijoohwee.github.io/docs/documents/knowgrph-agentic-os-follow-on-prd-tad.md` | Publish-doc MCP entry-point note | Documentation cross-link pass | Publish-side Agentic OS follow-on PRD/TAD plus current onboarding docs | Safer MCP onboarding from adjacent publish docs | This is the highest-ROI remaining doc-only follow-up because the follow-on PRD/TAD is still a plausible landing page for MCP readers on the publish side; a short redirect keeps the endpoint boundary explicit without rewriting the whole document. | Next, audit adjacent shared topology docs for any direct MCP endpoint guidance that should also point to the onboarding index and install contract. | 2026-07-10 |
| MCP shared-topology doc pointer | Keep broad publish-side topology docs from becoming indirect or stale MCP onboarding entry points. | Add a short remote-MCP onboarding pointer to `hjh-topology-document.md` so readers who encounter MCP in the shared topology narrative are sent to the onboarding index and install contract for the canonical endpoint boundary. | `knowgrph/todo-log.md,huijoohwee.github.io/docs/documents/hjh-topology-document.md` | Shared-topology MCP pointer | Documentation cross-link pass | Shared publish topology doc plus current onboarding docs | Safer MCP onboarding from shared topology docs | This is the highest-ROI remaining doc-only follow-up because broad topology docs often become first-read context; adding the pointer keeps the MCP endpoint split explicit without turning the topology doc into a second onboarding spec. | Next, audit any remaining shared publish docs that mention MCP transport or endpoint surfaces and add the same pointer where it materially reduces inference. | 2026-07-10 |
| MCP source follow-on doc pointer | Keep the SSOT follow-on Agentic OS doc aligned with the current MCP onboarding path. | Add a short remote-MCP onboarding pointer to `knowgrph-agentic-os-follow-on-prd-tad.md` so source-side readers are sent to the onboarding index and install contract instead of inferring the endpoint boundary from broader Agentic OS narrative alone. | `knowgrph/{docs/documents/knowgrph-agentic-os-follow-on-prd-tad.md,todo-log.md}` | Source follow-on MCP pointer | Documentation cross-link pass | Source-side Agentic OS follow-on PRD/TAD plus current onboarding docs | SSOT-side MCP onboarding parity for follow-on docs | This is the highest-ROI remaining doc-only follow-up because `knowgrph/docs/**` is the SSOT; aligning the source follow-on PRD/TAD with the current onboarding path keeps source and publish guidance consistent without changing runtime claims. | Next, audit other source-side broad architecture docs that mention remote MCP surfaces and add the same pointer only where it materially reduces inference. | 2026-07-10 |
| MCP source main-architecture doc pointer | Keep the main SSOT Agentic OS architecture doc aligned with the current MCP onboarding path. | Add a short remote-MCP onboarding pointer to `knowgrph-agentic-os-prd-tad.md` so readers who land on the primary Agentic OS topology spec are sent directly to the onboarding index and install contract for the canonical endpoint boundary. | `knowgrph/{docs/documents/knowgrph-agentic-os-prd-tad.md,todo-log.md}` | Source main-architecture MCP pointer | Documentation cross-link pass | Source-side Agentic OS main PRD/TAD plus current onboarding docs | SSOT-side MCP onboarding parity for the primary architecture doc | This is the highest-ROI remaining doc-only follow-up because the main Agentic OS PRD/TAD is a broad first-read SSOT entry point and already explains the four-surface MCP topology; adding the pointer keeps endpoint-boundary guidance explicit without rewriting architecture content. | Next, audit the remaining source-side infrastructure docs that mention Cloudflare or agent-ready MCP surfaces and add the same pointer only where it materially reduces inference. | 2026-07-10 |
| MCP source Cloudflare-baseline pointer | Keep the SSOT Cloudflare infrastructure baseline aligned with the current MCP onboarding path. | Add a short remote-MCP onboarding pointer to `knowgrph-cloudflare-document.md` so readers entering through the Cloudflare, DNS-AID, and agent-ready infrastructure narrative are sent to the onboarding index and install contract for the canonical endpoint boundary. | `knowgrph/{docs/documents/knowgrph-cloudflare-document.md,todo-log.md}` | Source Cloudflare MCP pointer | Documentation cross-link pass | Source-side Cloudflare baseline plus current onboarding docs | SSOT-side MCP onboarding parity for infrastructure docs | This is the highest-ROI remaining doc-only follow-up because the Cloudflare baseline is a realistic first-read infrastructure doc and already covers HTTP MCP, WebMCP, and DNS-AID surfaces; adding the pointer keeps endpoint-boundary guidance explicit without changing the infrastructure narrative. | Next, audit the remaining source-side publish-topology and infrastructure docs and add the same pointer only where it materially reduces inference. | 2026-07-10 |
| MCP source publish-topology pointer | Keep the SSOT cross-repo publish topology doc aligned with the current MCP onboarding path. | Add a short remote-MCP onboarding pointer to `knowgrph-cross-repo-publish-topology.md` so readers entering through the Pages/publish topology narrative are sent to the onboarding index and install contract for the canonical endpoint boundary. | `knowgrph/{docs/documents/knowgrph-cross-repo-publish-topology.md,todo-log.md}` | Source publish-topology MCP pointer | Documentation cross-link pass | Source-side cross-repo publish topology doc plus current onboarding docs | SSOT-side MCP onboarding parity for publish-topology docs | This is the highest-ROI remaining doc-only follow-up because the cross-repo publish topology doc is a realistic infrastructure entry point and already references Pages/MCP reads; adding the pointer keeps endpoint-boundary guidance explicit without changing topology ownership or deployment narrative. | Next, audit any remaining source-side agent-ready or API/integration docs that mention MCP surfaces and add the same pointer only where it materially reduces inference. | 2026-07-10 |
| MCP source agent-ready PRD/TAD pointer | Keep the main source-side agent-ready PRD/TAD aligned with the current MCP onboarding path. | Add a short remote-MCP onboarding pointer to `knowgrph-agent-ready-prd-tad.md` so readers entering through the shipped Pages MCP and WebMCP narrative are sent directly to the onboarding index and install contract for the canonical endpoint boundary. | `knowgrph/{docs/documents/knowgrph-agent-ready-prd-tad.md,todo-log.md}` | Source agent-ready MCP pointer | Documentation cross-link pass | Source-side agent-ready PRD/TAD plus current onboarding docs | SSOT-side MCP onboarding parity for agent-ready docs | This is the highest-ROI remaining doc-only follow-up because the main agent-ready PRD/TAD is a realistic first-read source doc for the shipped MCP surface; adding the pointer keeps endpoint-boundary guidance explicit without changing the read-only Pages/WebMCP contract narrative. | Next, audit the remaining source-side agent-ready runtime and API/integration docs that mention MCP surfaces and add the same pointer only where it materially reduces inference. | 2026-07-10 |
| MCP source agent-ready runtime pointer | Keep the source-side runtime companion for the shipped agent-ready surface aligned with the current MCP onboarding path. | Add a short remote-MCP onboarding pointer to `knowgrph-agent-ready-prd-tad.runtime.md` so readers entering through the route-contract and deployment companion are sent directly to the onboarding index and install contract for the canonical endpoint boundary. | `knowgrph/{docs/documents/knowgrph-agent-ready-prd-tad.runtime.md,todo-log.md}` | Source agent-ready runtime MCP pointer | Documentation cross-link pass | Source-side agent-ready runtime companion plus current onboarding docs | SSOT-side MCP onboarding parity for runtime docs | This is the highest-ROI remaining doc-only follow-up because the runtime companion is a direct route-contract doc for the shipped Pages MCP surface; adding the pointer keeps endpoint-boundary guidance explicit without changing runtime diagrams or deployment guidance. | Next, audit the remaining source-side API and integration docs that mention MCP surfaces and add the same pointer only where it materially reduces inference. | 2026-07-10 |
| Remote MCP grammar invocation for agentic-canvas-os | Transition from local static grammar compilation to dynamic remote grammar resolution over MCP. | Update agentic-canvas-os to query knowgrph remote MCP (/api/invoke) for grammar (/, @, #) without local mutation or static compilation. | `agentic-canvas-os/{wrangler.jsonc,web/build.mjs}` | MCP remote overlay, Cloudflare Worker route | `fetch(/api/invoke)` with AbortController | User grammar inputs | Dynamic command suggestions via headless UI | Rely on remote SSOT from knowgrph control-plane MCP; minimize TCO and avoid legacy static artifact generation. | Review AgenticRAG schema for potential updates due to architectural shift. | 2026-07-10 |
| MCP protocol handshake fix & Dynamic SSOT Edge caching | Ensure robust remote MCP connectivity and ingest live GitHub markdown into the remote knowgrph control plane | Implement `initialize` handshake in `knowgrph-mcp-client.js` to capture `mcp-session-id`, and update `knowgrph` Worker to dynamically fetch Edge-cached markdown from GitHub for CMDK payloads. | `agentic-canvas-os/src/knowgrph-mcp-client.js`, `knowgrph/{mcp/agentic-canvas-os-docs-core.mjs,cloudflare/workers/knowgrph-mcp/tool-registry.mjs}` | MCP client session state, Dynamic payload builder | `ensureSession`, `buildAgenticCanvasOsDocsDynamicResolutionPayload` | Grammar token query | Stateful HTTP headers, dynamically parsed markdown from GitHub | Use native Cloudflare Edge caching for high-ROI GitHub fetch to keep token cost zero; fail-closed offline testing preserved via `fetchImpl` injection. | Wire the updated `knowgrph-mcp-client.js` directly into the `knowgrph` CMDK composer frontend UI. | 2026-07-10 |

## 2026-07-11

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Airvio Markdown agent discovery | Make public discovery cheap, deterministic, and free of SPA or empty-document noise. | Generate root `llms.txt` and sitemap entries upstream, redirect legacy machine paths, and filter zero-length documents in the D1 crawler query. | `knowgrph/{cloudflare/pages/knowgrph-agent-ready.mjs,cloudflare/workers/knowgrph-storage/db.ts,canvas/src/__tests__,docs/documents/knowgrph-agent-ready-document.md}` + generated `huijoohwee` artifacts | Agent-ready Pages + storage discovery SSOT | `buildAgentReadyStaticFiles`, `readCrawlerDocumentRows` | Shared route metadata and D1 document rows | Canonical text, XML, redirects, and non-empty Markdown indexes | One generated discovery owner prevents duplicated catalogs while database filtering keeps valid blank editor files out of crawler prompts. | Deploy the validated Pages and storage Worker artifacts, seed docs, then rerun live agent-ready and header checks. | 2026-07-11 |
| Knowgrph embeddability boundary | Turn the iframe-first recommendation into one source-owned contract before host integrations drift. | Publish a canonical embeddability contract that keeps `iframe` as the live embed SSOT, reserves `postMessage` for thin host control, treats host `<canvas>` as visual-only projection, and records the current CSP-owner reconciliation follow-up without touching runtime code. | `knowgrph/{docs/documents/knowgrph-embeddability-contract.md,README.md,todo-log.md}` | Embed boundary + README discoverability | Embeddability contract publication | Existing `/doc-view` embed helpers, `_headers` frame policy, and root Pages frame-ancestor override | Source-owned embeddability contract plus README pointer and ledger trace for the next runtime-safe embed slice | Since the repo already ships a run-scoped iframe embed path, the highest-ROI doc-only move is to freeze that boundary now so future host integrations reuse the upstream runtime instead of inventing a second interactive renderer contract. | When code edits reopen, reconcile the root `frame-ancestors` policy with the narrower `/knowgrph*` and `/knowgrph/doc-view` policy, then add a small postMessage contract and focused browser proof. | 2026-07-11 |
| Responsive parity release gate | Turn Dev -> Prod -> Cloudflare responsive parity from a recommendation into an explicit release blocker. | Update the mobile-first plan and cross-repo publish topology so mobile keyboard proof, route-and-action review, and publish sync become the named responsive parity gate before `pages:deploy-cloudflare`. | `knowgrph/{docs/documents/{knowgrph-mobile-first-enhancement-plan.md,knowgrph-cross-repo-publish-topology.md},todo-log.md}; agentic-canvas-os/docs/kanban.md` | Responsive parity release contract | Responsive parity blocker definition | Existing mobile workflow matrix, mobile keyboard browser smoke, and publish sync gate | Source-owned release wording that blocks publish when touch-first proof and mirror parity drift apart | After route-and-action evidence and live grammar proof are in place, the highest-ROI cleanup is to make responsive parity a named release blocker so mobile proof cannot silently degrade at publish time. | Next, mirror the validated responsive parity wording into AgenticRAG notes only after the source-owned docs map is rechecked and any publish-side mirror wording is intentionally updated. | 2026-07-11 |
| Mobile route-and-action evidence slice | Turn mobile performance judgement into one source-owned workflow matrix before broader publish-parity claims advance. | Materialize the top phone workflow matrix in the feature map, align the UI/UX contract with its immediate/deferred/fallback-safe classes, and make the cross-repo publish topology treat that matrix as a release contract for heavy-runtime mobile flows. | `knowgrph/{docs/documents/{knowgrph-feature-map.md,knowgrph-ui-ux-design-document.md,knowgrph-cross-repo-publish-topology.md},todo-log.md}; agentic-canvas-os/docs/kanban.md` | Mobile workflow evidence + release contract | Route-and-action evidence matrix | Existing mobile-first enhancement plan, heavy-runtime gating policy, keyboard-proof slice, and current feature-map chunk analysis | Source-owned workflow matrix and release-contract wording that tell operators exactly which phone actions may wake Monaco, Mermaid, MapLibre, or Three and when that is allowed | After keyboard reachability is proven, the highest-ROI move is to convert mobile runtime decisions into one workflow matrix so future mobile work and publish parity claims are evidence-driven instead of intuition-driven. | Next, promote Dev -> Prod -> Cloudflare responsive parity into the release gate by linking the new workflow matrix to focused publish validation and any future AgenticRAG mirror note updates. | 2026-07-11 |
| Agentic Canvas OS next ROI slice | Prioritize the next min-viable-max-value doc-owned execution queue after the AI Gateway no-secret slice. | Record dynamic remote MCP grammar resolution and mobile virtual-keyboard proof as the next paired runtime slice, with Agentic Canvas OS docs owning the queue and `knowgrph` mobile docs remaining the proof source. | `knowgrph/{todo-log.md,docs/documents/knowgrph-mobile-first-enhancement-plan.md}; agentic-canvas-os/docs/{kanban.md,PRD-TAD.md,RUNTIME-READINESS.md}` | Agentic Canvas OS roadmap + mobile-first proof queue | Next-slice prioritization | Current Agentic Canvas OS grammar direction, mobile-first enhancement plan, and no-secret AI Gateway completion state | Source-owned planning rows and runtime docs that make the next grammar-plus-mobile execution slice explicit without opening code edits yet | With the AI Gateway doc-only slice complete, the highest-ROI next move is to centralize grammar resolution on the remote MCP SSOT and promote mobile keyboard proof as the gating evidence path for touch-first execution. | When code edits reopen, start with remote grammar resolution in `agentic-canvas-os` and the mobile virtual-keyboard proof harness in `knowgrph`, then mirror any validated contract changes into AgenticRAG only after source proof passes. | 2026-07-11 |
| AI Gateway analytics and legacy endpoint audit | Complete the no-secret execution queue by materializing analytics proof and tightening legacy API wording without code edits. | Document the `docs/reports/ai-gateway-cost-proof.md` structure and update the API document to explicitly contrast legacy `gateway.ai.cloudflare.com` with the modern `api.cloudflare.com/.../ai` REST endpoints. | `knowgrph/{docs/documents/knowgrph-api-document.md,docs/reports/ai-gateway-cost-proof.md,todo-log.md}` | AI Gateway analytics proof + endpoint wording | Legacy endpoint audit and cost proof creation | Existing AI Gateway enhancement plan and API document | Source-owned analytics cost proof artifact and explicit legacy vs. modern REST API endpoint boundaries in the API doc | Since the AI Gateway enhancement plan prioritizes proof density and modernization clarity before touching runtime code, materializing the cost proof and tightening the endpoint wording finishes the current no-secret ROI slice. | When live activation is reopened, add an accepted AI Gateway secret to the `joohwee` Pages project and rerun `npm run ai-gateway:readiness:check` without skip flags to unblock the live transport smoke test and dynamic route deployment. | 2026-07-11 |
| AI Gateway no-secret readiness branch | Keep the deferred-secret operator path implementation-accurate while live Pages activation is intentionally skipped. | Document that `npm run ai-gateway:readiness:check -- --skip-sync-check --skip-live` still enforces Pages secret-list verification, record that source proofs plus publish proxy smoke currently pass, and prioritize a source-owned analytics proof plus endpoint wording audit as the next no-secret ROI slice. | `knowgrph/{docs/documents/{knowgrph-ai-gateway-enhancement-plan.md,knowgrph-api-document.md,knowgrph-cloudflare-document.md,knowgrph-cross-repo-publish-topology.md},todo-log.md}` | AI Gateway readiness docs | No-secret operator verification contract | Current readiness command output, AI Gateway enhancement plan, API contract, and publish-topology gate docs | Source-owned docs aligned with actual skip-live behavior and the next no-secret execution queue | Since the user is explicitly deferring Pages AI Gateway secret setup, the highest-ROI move is to keep the operator path fail-closed and implementation-accurate while teeing up analytics-proof and endpoint-modernization work that does not depend on live secret rollout. | When live activation is reopened, add an accepted AI Gateway secret to the `joohwee` Pages project and rerun `npm run ai-gateway:readiness:check` without skip flags; until then, materialize the small source-owned analytics proof artifact and tighten legacy endpoint wording. | 2026-07-11 |

## 2026-07-13

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| FloatingPanel prompt-preset selection, activation, and execution boundary | Let users select Video Agent, SME Care Agent, or Investment Research Agent before loading one centralized prompt, while keeping video execution source-backed and preserving generic slash-agent routing for the other two. | Replace the video-only control with a shared native selector; read labels, routes, descriptions, activation modes, and prompt text only from `agentic-canvas-os/docs/PROMPT-PRESETS.md`; fail closed on missing/duplicate/invalid entries; keep Load zero-spend and Send approval-gated. For video, defer empty host-file creation; await exact Markdown applies; reject stale editor sync; prove the runnable text/image/video plan; apply provider plus typed thinking/token-cap settings; require the eight-sheet text package; hand the committed graph to the shared Run all owner; persist and project returned media through existing owners. | `knowgrph/{canvas/src/features/chat,canvas/src/features/canvas,canvas/src/features/workspace-fs,canvas/src/features/markdown-workspace,canvas/src/components/StoryboardWidgetCanvas,canvas/src/hooks/store/graph-data-slice,canvas/src/lib/storage,canvas/src/__tests__,docs/documents/knowgrph-api-document.md,todo-log.md}; huijoohwee/docs/knowgrph-agentic-video-canvas-demo.md; agentic-canvas-os/docs/{PROMPT-PRESETS.md,RUNTIME-PROOF.md}` | Central prompt-preset catalog + selector + slash-agent submit + video Run all + source projection | `loadPromptPresetCatalog`, `loadPromptPreset`, `loadVideoAgentDemoPreset`, `resolveVideoAgentPresetExecutionPreflight`, `parseGenerationInvocation`, `parseChatSkillSlashInvocation`, `requestWorkflowRunAllFromCommittedCanvas`, `resolveStoryboardWidgetWorkflowRunGraphSnapshot` | Selected centralized preset id plus active workspace context; video additionally requires its authored Canvas/script sources and credential | Selector exposes exactly the three requested agents; each loaded prompt begins with its registered slash route; prompt text has one central owner; video retains `#thinking.type.enabled`, Medium 16,384-token cap, eight sheets, committed graph activation, durable artifacts, and shared projections | Load performs zero model/provider calls. Send is explicit approval. Invalid catalog, unavailable source, missing credentials, graph commit failure, capability gap, or missing Run all owner stops visibly before provider spend; historical artifacts remain read-only. | Preserve failed historical folders unchanged; repeat provider proof only when runtime capability changes. | 2026-07-13 |
| FloatingPanel Chat thread continuity | Restore the pending assistant bubble and streaming tail follow. | Materialize one assistant row before the first token, stream into it in place, preserve pre-update tail-follow across large chunks, and let manual scrolling pause follow until the next stream. | `knowgrph/{canvas/src/features/chat,canvas/src/__tests__,docs/documents,todo-log.md}; huijoohwee.github.io/schema/AgenticRAG` | Shared chat thread + follow state | `ChatMessageRow`, `useFloatingPanelChatThreadFollow` | Optimistic turn, cumulative stream revisions, user scroll position | One pending/live/final bubble with chronological status and bounded auto-follow | Pending/live/final identity stays keyed by assistant id; follow intent is sampled from scroll events, not post-growth geometry. | Re-run focused streaming tests, TypeScript, hygiene, AgenticRAG JSON validation, and a provider-free browser harness after future thread changes. | 2026-07-13 |
| Agentic Video Canvas default runtime recovery | Restore the source-backed demo and keep generated media visible after provider success. | Route canonical script bindings before generic chat, recover BytePlus activation failures through bounded model discovery, use registered generation forms, and project one persisted media identity into Widgets and Rich Media Panels. | `knowgrph/{canvas/src,docs/documents,todo-log.md}; huijoohwee/docs/knowgrph-agentic-video-canvas-demo.md` | Chat transport + workflow media projection + default seed | `executeChatSubmitTransportAttempt`, `buildRichMediaWidgetOutputPatch`, `publishMediaRunOutputToRichMediaPanel` | `/video-agent` source binding, provider response, generated artifacts | Local preset activation, bounded provider fallback, durable text/image/audio/video projections | Source binding is the route SSOT; runtime artifacts remain blank until provider return and storage read-back; no chat-log backfill or deploy. | Run an explicitly approved bounded provider smoke only after model entitlement is confirmed; then record live manifest, playback, cost, and Canvas reload proof. | 2026-07-13 |

## 2026-07-14

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Monthly TODO shard architecture | Bound planning context and reduce multi-writer conflicts without adding infrastructure. | Route the bounded TODO index to monthly cross-repository shards; keep closed months immutable, append new rows only to the active month, validate shard identity and size, and retrieve exact scope/month before broader search. | `agentic-canvas-os/{docs,todo}` | TODO index and monthly planning shards | `TODO.md`, `todo/YYYY-MM.md`, planning compliance gates | Cross-repository planning directive and UTC work date | Bounded append-only monthly ledger with on-demand retrieval | Flat Markdown remains the minimum-viable maximum-value owner while one scope stays below the shard caps; scope directories require measured pressure and a versioned migration. | Run structural and base-prefix gates before release; introduce BM25 only after exact search becomes noisy. | 2026-07-14 |
| Runtime-ready Dev to Prod Cloudflare release | Promote the merged source-backed invocation catalog through the canonical mirror and deployment workflow. | Promote only the merged Dev SHA after protected integration, canonical mirror synchronization, source and asset parity checks, one verified Cloudflare deployment, and probes for both production routes; stop immediately on any mismatch or unavailable credential. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `knowgrph/{todo-log.md,package.json}`; `huijoohwee/content/knowgrph` | Release workflow ledger | `RELEASE-WORKFLOW.md`, planning compliance gates, publish and deploy commands | Merged Dev SHA, canonical SSOT revision, clean Prod mirror, and deployment authorization | Auditable promotion record with immutable deployment and route verification evidence | Treat upstream source, generated mirror, and deployment as separate gates; generated provider evidence remains excluded from release inputs. | Complete preflight, validate Dev and mirror parity, deploy once through the repository-owned command, then record immutable route evidence. | 2026-07-14 |
| Production path hygiene corrective release | Remove machine-specific filesystem paths from published assets and active documentation without rewriting immutable history. | Normalize active sources, fixtures, API docs, and HackaMap manifests to repository-relative or `$GITHUB_ROOT` references; gate published output, regenerate mirrors from upstream owners, and redeploy only after protected integration and parity checks. | `agentic-canvas-os/{todo/2026-07.md,docs/RELEASE-WORKFLOW.md}`; `knowgrph/{scripts,docs,data/test-data,todo-log.md}`; `hackamap/site/hackamap_query_runner.py` | Release path hygiene contract | `check-conflict-compliance.mjs`, `hackamap_query_runner.py` | Canonical sources, generated manifests, and promoted SHAs | Path-neutral production artifacts and live parity proof | Preserve immutable ledgers; block device paths at source and serialization boundaries; keep the operator-owned x402 address gate separate. | Run focused validation, protected integration, mirror regeneration, one deployment, and live leak probes. | 2026-07-14 |
| Current Dev to Prod Cloudflare parity release 2026-07-14 | Publish the exact protected Dev tip after preserving unrelated in-flight work. | Validate the isolated Dev candidate, merge only ledger evidence through protected integration, regenerate the Prod mirror canonically, deploy one verified snapshot, seed required storage, and prove both production routes and shipped asset parity. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `knowgrph/{todo-log.md,package.json}`; `huijoohwee/content/knowgrph` | Release parity ledger | `ci:integration`, `pages:build-sync`, `pages:deploy-cloudflare` | Protected Dev SHA, clean Prod mirror, operator deployment approval | Auditable Dev, Prod, and Cloudflare parity evidence | Preserve concurrent work on pushed handoff branches; release only merged source; stop on the first failed gate or partial deployment. | Complete protected integration, canonical promotion, one deploy, storage reconciliation, and live route and asset verification. | 2026-07-14 |
| Legacy todo and parity worktree cleanup | Remove completed release worktrees and eliminate duplicate Knowgrph planning owners. | Remove the two merged parity worktrees without force; delete both repository-local todo files and their organizer; route active docs, validation, conflict checks, and future task rows exclusively through Agentic Canvas OS `TODO.md` and the active monthly shard. | `agentic-canvas-os/{docs,todo/2026-07.md}`; `knowgrph/{docs,goal.md,knowgrph_parser,canvas/src/__tests__,scripts}`; `huijoohwee.github.io/schema/AgenticRAG` | Central planning and worktree hygiene | `git worktree remove`, `TODO.md`, planning compliance gates | Merged parity worktrees plus duplicate local planning files | Pruned worktrees and one bounded cross-repository planning SSOT | Both tips are already merged; monthly shards provide the strict row, lifecycle, retrieval, and byte-prefix guarantees required for planning history. | Keep release branches until separately authorized for deletion; append future work only to the active shard. | 2026-07-14 |
| Config path and shared export failure repair | Restore the full configuration-path suite without masking generated artifacts or package modules. | Exclude ignored generated preview roots from canonical reference scans, include `.tsx` modules in shared-package source discovery, and export the existing RAF runtime through the `grph-shared` package map. | `knowgrph/{knowgrph_parser/config_paths_test.py,grph-shared/package.json}`; `agentic-canvas-os/todo/2026-07.md` | Path integrity and shared package surface | `prune_ignored_ref_scan_dirs`, `test_grph_shared_package_exports_match_source_modules` | Ignored preview artifacts plus TypeScript and TSX source modules | Green canonical fixture-reference and package export coverage | Generated previews are not source authority; every authored TS or TSX shared module must match one public package export. | Keep ignored outputs outside source scans and rerun the 14-test suite after shared-module additions. | 2026-07-14 |
| Native image to Three.js skill | Convert supported image sources into one reusable Three.js projection without adding an agent or external plugin dependency. | Add the model-free `image.to-threejs` skill, typed conversion manifest, native raster and SVG loaders, and one shared render mode across Cards, Widgets, Storyboard overlays, and Rich Media Panels; reuse existing skill, semantic, image, workflow, and proof owners. | `agentic-canvas-os/docs/{SKILLS.md,IMAGE-TO-THREEJS-SKILL.md}`; `knowgrph/{canvas/src,docs/documents/knowgrph-api-document.md}` | Image to Three.js skill contract and shared media projection | `buildImageToThreeJsConversion`, `ImageThreeJsSurface`, `runStoryboardWidgetMediaWorkflowNode` | One PNG, JPG, JPEG, or SVG source from local or connected widget data | Typed zero-cost manifest and native Three.js Card, Widget, overlay, and Rich Media Panel render | Vector sources become bounded shape and stroke geometry; raster sources become color-correct textured planes; typed errors fail before spend; external Object Sculptor code and dependencies remain forbidden. | Add focused browser proof for raster and SVG Rich Media rendering, then mirror only the source-backed contract into AgenticRAG after Dev integration. | 2026-07-14 |
| SME growth-stage risk and coverage copilot runtime | Implement the PRD's minimum viable local risk copilot while preserving regulated-action and deployment boundaries. | Reuse the typed SME and probe-tree owners; add redaction, declared trigger evaluation, draft-only nudges, approval-gated category matching, multilingual fallback, read-only status views, and focused exact-zero-cost proof without Prod or Cloudflare mutation. | `knowgrph/{contracts,mcp,canvas/src/__tests__,docs}`; `agentic-canvas-os/todo/2026-07.md` | SME risk-copilot runtime and status surface | `sme_care_agent_status`, source normalization, trigger evaluation, nudge drafting, marketplace matching, multilingual adaptation | Synthetic or operator-redacted SME profile, REG delta, requested language, and optional canonical approval | Typed exposure, declared trigger, draft nudge, category handoff, fallback metadata, status views, and cost logs | Pure validation and deterministic rules own the default path; unsafe input fails before persistence or spend, unmatched deltas no-op, and regulated outputs remain approval-gated and non-bindable. | Run focused contract, MCP, property, Canvas, hygiene, and affected-scope checks; retain Dev-only boundaries and record runtime proof. | 2026-07-14 |
| Agentic Canvas OS multi-device collaboration protected release 2026-07-14 | Release the collaboration runtime and serialized multi-device delivery lane through protected integration. | Fix room authorization and offline replay, validate Durable Object persistence and CI contracts, publish one scoped branch, merge only after required checks, deploy the merged SHA once, and verify the live readiness and collaboration routes. | `agentic-canvas-os/{agent-api,src,worker,web,.github,scripts,docs,todo}` | Collaboration runtime and protected delivery pipeline | `createAuthSessionHandler`, `CanvasRoom`, device publish and deployment workflows | Capability-strength room identifier, queued operations, protected branch, and operator deployment approval | Scoped session tokens, durable idempotent operations, merged Dev SHA, serialized Cloudflare deployment, and live verification evidence | Room identifiers are shareable capabilities; anonymous callers cannot select identity or obtain unscoped room access; offline operations persist until acknowledged; production uses only the protected merged SHA. | Stop on authorization, append-only history, CI, credential, deployment, or live verification failure and report the exact blocked stage. | 2026-07-14 |
| Agentic Canvas OS single-worktree consolidation and protected synchronization 2026-07-14 | Consolidate concurrent repository work into one canonical checkout and synchronize it with protected `origin/main`. | Preserve every unique commit, resolve the collaboration contract at its source, remove the clean redundant worktree, validate the combined branch, publish it through one protected pull request, and fast-forward the canonical checkout after merge. | `agentic-canvas-os/{.github,COLLABORATION.md,__tests__,docs,scripts,src,todo,worker}` | Single-worktree delivery and synchronization contract | `device:publish`, `sync:live`, protected auto-merge | Two clean branch histories, one in-progress merge, fetched `origin/main`, and operator consolidation authority | One validated task branch, one registered canonical worktree, protected merge commit, and clean local `main` parity | Unique commits remain immutable inputs; direct `main` pushes, force operations, secondary worktrees, unexplained dirt, and non-fast-forward pulls fail closed. | Monitor required checks, merge through the protected gate, then run `sync:live` and prove local `HEAD` equals fetched `origin/main`. | 2026-07-14 |
| Canonical Dev explicit fast-forward startup | Prevent recurring clean-main startup failures after protected merges without weakening source identity. | Add one explicit two-phase command that preflights every canonical source, rejects dirt, task branches, divergence, and redundant worktrees, then applies only fast-forward merges before the unchanged Dev gate. | `agentic-canvas-os/{docs/FACTS.md,docs/START-WORKFLOW.md,todo/2026-07.md}`; `knowgrph/{package.json,scripts,docs/collaboration-runtime-contract.md}` | Canonical Dev source synchronization | `syncDevCanonicalSources`, `npm run dev:latest` | Clean canonical checkouts whose fetched remote refs may have advanced | Safely refreshed canonical sources followed by ordinary fail-closed Dev startup | A complete preflight precedes every mutation, so one invalid source prevents partial updates; task branches retain their explicit task-mode workflow. | Keep normal `npm run dev` non-mutating and use `dev:latest` only for clean canonical branch refreshes. | 2026-07-14 |
| Knowgrph collaboration report source-revision binding 2026-07-14 | Prevent replay of a valid collaboration report and result pair from another source commit. | Record the merged `sourceRevision` and exact-byte report binding; keep schemas and verifier code upstream; mirror only consumer guidance; preserve unrelated work in its named stash; forbid Prod or Cloudflare mutation. | `knowgrph/{schemas,scripts,.github/workflows,docs}`; `huijoohwee.github.io/schema/AgenticRAG`; `agentic-canvas-os/todo/2026-07.md` | Collaboration report provenance contract | `resolveCollaborationRuntimeSourceRevision`, `validateCollaborationRuntimeValidationPair`, `collaboration:report:check-result` | Canonical report bytes, validation envelope, pull-request head SHA, and fetched repository state | Schema-valid report and result artifacts bound to exact bytes and one expected source commit | Local runs derive repository `HEAD`; CI supplies the pull-request head or push SHA and rejects digest, pair-revision, or expected-revision mismatch before the canonical gate. | Add canonical `sourceRepository` to the report and envelope, then compare it with `github.repository` so a valid pair from a fork with the same commit SHA cannot be replayed. | 2026-07-14 |
| Image to Three.js runtime-ready Dev promotion | Promote the validated native image projection without widening its owners or deployment scope. | Record focused and browser proof for PNG, runtime-generated JPEG, SVG, fallback, source replacement, and disposal; promote the canonical skill contract to runtime-ready Dev; mirror only the source-backed consumer contract into AgenticRAG. | `agentic-canvas-os/{docs,todo/2026-07.md}`; `knowgrph/{canvas/src,docs/documents/knowgrph-image-to-threejs-api.md}`; `huijoohwee.github.io/schema/AgenticRAG` | Image to Three.js proof and schema mirror | `buildImageToThreeJsConversion`, `ImageThreeJsSurface`, `RichMediaBrowserSmokePage` | Validated Knowgrph Dev diff and PNG, JPEG, or SVG source | Runtime-ready Dev skill contract plus bilingual Card, Widget, Storyboard, and Rich Media Panel consumer guidance | The source runtime remains canonical; downstream docs describe only proven behavior; no alias, copied external implementation, additional dependency, Prod mirror, or Cloudflare claim is allowed. | Integrate the validated Knowgrph source branch through its protected gate, then reconcile the exact merged source SHA before any separately approved release. | 2026-07-14 |

## 2026-07-15

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Storyboard media projection and Rich Media resize release 2026-07-15 | Promote the validated shared inline-media, preview, graph-materialization, and Rich Media resize repairs through protected Dev and one production deployment. | Preserve generated workspace notes outside the release; commit the shared owners; validate focused Canvas, TypeScript, hygiene, and integration gates; merge only after the protected gate; synchronize the canonical mirror; deploy the exact merged SHA once; then verify both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `knowgrph/{canvas/src,package.json,.github/workflows/release.yml}`; `huijoohwee/content/knowgrph` | Shared media projection and release ledger | `CardInlineTextProjectedMediaChip`, `StoryboardCardMediaDropSlot2d`, `FlowCanvasMediaOverlays`, `resolveFlowCanvasMediaOverlayInteractionPolicy`, `Production Release` | Validated Knowgrph task branch, Agentic Canvas OS planning row, protected Dev SHA, and operator deployment authorization | Merged source fix, generated mirror parity, one Cloudflare deployment, and verified `airvio.co` plus `/knowgrph` routes | Cards, Widgets, and Rich Media Panels share canonical media and resize owners; no direct main push, duplicate projection, manual mirror patch, or deployment before protected integration. | Record the merge SHA, deployment run, route probes, and any environment-only blocker; leave unrelated generated notes in their named stash. | 2026-07-15 |
| Widget Probe Tree Rich Media multi-turn release 2026-07-15 | Promote the source-backed Widget Card probe invocation, multi-turn Rich Media response, retained edge, and responsive viewport layout. | Preserve unrelated work on named branches; rebase onto the shared media owner; validate probe, text generation, Canvas, type, hygiene, and integration gates; merge through protected Dev; regenerate the canonical mirror; deploy the merged SHA once; verify both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `knowgrph/{canvas/src,docs,.github/workflows/release.yml}`; `huijoohwee/content/knowgrph` | Widget probe workflow and release ledger | `runStoryboardProbeTreeInvocation`, `publishStoryboardWidgetRichMediaOutput`, `fitStoryboardWidgetWorkflowViewport`, `Production Release` | Rebasing task branch, SME Care Agent validation fixtures, protected Dev SHA, clean Prod mirror, and deployment authorization | Retained Widget Card and edge, Rich Media-only streamed output, multi-select and multi-turn continuation, auto-fit layout, exact promoted SHA, and verified production routes | Shared probe, widget, graph, Rich Media, and viewport utilities remain authoritative; provider output never replaces the input Widget Card; no direct main push, manual mirror edit, duplicate text widget, or deployment before protected integration. | Merge the planning row and validated Dev branch, promote canonically, deploy once, and record immutable route and SHA parity evidence. | 2026-07-15 |
| FloatingPanel prompt preset alias contract 2026-07-15 | Synchronize Agentic Canvas OS with the first-class Knowgrph Prompt Presets surface. | Define catalog-owned `/*-prompt-preset` selection aliases separately from executable `/...-agent` routes; add Crawler Agent; preserve zero-spend load, no-submit selection, source-backed prompt ownership, and Dev-only boundaries. | `agentic-canvas-os/{docs/FACTS.md,docs/PROMPT-PRESETS.md,docs/RUNTIME-PROOF.md,todo/2026-07.md}` | Prompt preset catalog and proof contract | `prompt_presets`, `slash_command`, `runtime_command` | Four source-backed FloatingPanel presets and their executable runtime routes | Four alias-to-runtime mappings with centralized prompts and documented execution boundary | The catalog owns selection aliases; skills and the command dictionary continue to own execution routes, preventing preset labels from silently redefining runtime commands. | Keep future preset additions source-backed, suffix-consistent, independently validated, and non-submitting until the operator explicitly sends. | 2026-07-15 |
| Native image to Three.js and GLB protected release 2026-07-15 | Promote the reusable image conversion, procedural GLB, shared Rich Media interaction, and Widget Card invocation owners through protected integration. | Validate canonical invocation docs, procedural construction and exporter fidelity, Canvas interaction ownership, TypeScript, hygiene, and integration gates; merge Agentic Canvas OS first, merge Knowgrph second, then deploy the exact protected Dev SHA once and verify production parity. | `agentic-canvas-os/{docs,todo/2026-07.md}`; `knowgrph/{canvas/src,grph-shared/src,docs,.github/workflows/release.yml}`; `huijoohwee/content/knowgrph` | Image conversion and protected release ledger | `image.to-threejs`, `image.to-glb`, `ImageToGlbSurface`, `Production Release` | Authorized task branches, one supported image source, validated procedural artifacts, protected Dev SHA, and production environment approval | Canonical invocation catalog, separate Three.js and GLB Rich Media outputs, local orbit and pan controls, exact promoted SHA, and verified production routes | Source media remains unchanged; shared resolvers and interaction guards stay authoritative; procedural code replaces baked geometry; direct main pushes, external copied dependencies, manual mirror edits, and deployment before protected integration remain forbidden. | Merge the Agentic contract PR, validate and merge the Knowgrph runtime PR, dispatch the manual production workflow once, and record route, asset, deployment, and residual-risk evidence. | 2026-07-15 |
| Prompt invocation and workflow output repairs protected release 2026-07-15 | Promote the accumulated browser-comment repairs for prompt invocation and source-backed workflow outputs through protected Dev and production gates. | Publish the prompt preset, invocation token, native crawler, Widget input-topology, Rich Media output, thinking-budget, and Probe-Tree repairs through protected integration; preserve workspace notes; promote only the merged SHA; deploy once; verify both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `knowgrph/{canvas/src,package.json,scripts,.github/workflows/release.yml}`; `huijoohwee/content/knowgrph` | Prompt and workflow release ledger | `resolveChatComposerTrigger`, `runStoryboardWidgetNativeCrawlerInvocation`, `preserveStoryboardWidgetWorkflowInputTopology`, `materializeStoryboardWidgetProbeTreeInvocation`, `Production Release` | Attributed Knowgrph task diff, appended planning row, protected merge SHA, clean generated mirror, and operator release authorization | Invocable slash, at, and hash tokens; durable Widget and edge topology; bounded source-backed output; Probe-Tree branches; exact promoted SHA; verified production routes | Shared invocation, crawler, graph, Rich Media, and Probe-Tree owners remain authoritative; no backfill, direct main push, manual mirror edit, paid proof call, or deployment before protected integration. | Merge this planning row first, validate and merge Knowgrph through the Integration Gate, regenerate the mirror canonically, deploy once, and record immutable SHA and route evidence. | 2026-07-15 |

## 2026-07-16

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| XR and Camera shared runtime consolidation 2026-07-16 | Keep Canvas XR Mode, FloatingPanel Camera, and FloatingPanel XR synchronized through one camera-framing authority. | Consolidate board selection, camera draft, graph persistence, live Three camera application, reverse orbit sync, axis recenter, and XR control profiles into shared owners; delete BottomPanel XR and stale duplicate projections. | `knowgrph/{canvas/src,docs}`; `agentic-canvas-os/todo/2026-07.md`; `huijoohwee.github.io/schema/AgenticRAG` | Shared Camera Framing Owner | `StrybldrCameraFramingSection`, `cameraFramingRuntime`, `cameraFramingPose`, `cameraFramingControlsRuntime`, `cameraControlsProfile` | Camera panel changes, XR axis commands, manual OrbitControls motion, document changes, and Reframe requests | One synchronized camera snapshot across Canvas XR Mode and both FloatingPanel projections, with graph history written only by Reframe | Controls remains the sole live camera writer; thin panels project shared state; repeated axis commands recenter; reverse sync waits for damping; stale BottomPanel and duplicate selector paths are removed. | Merge the protected Knowgrph PR, regenerate the AgenticRAG docs map from the merged source revision, and keep Prod and Cloudflare unchanged until separately authorized. | 2026-07-16 |
| Cross-device runtime revision identity and catalog freshness enforcement 2026-07-16 | Prevent same-branch devices from claiming parity while serving different app, docs, or catalog revisions. | Require exact Knowgrph and Agentic Canvas OS SHAs across devices, expose revision-bearing runtime identity, bind catalog hydration to the docs SHA, and allow at most two explicit refresh attempts before a visible stale or blocked result. | `agentic-canvas-os/{docs/{AGENTS.md,FACTS.md,START-WORKFLOW.md,VALIDATION-RUNBOOK.md,RELEASE-WORKFLOW.md,RUNTIME-PROOF.md},todo/2026-07.md}` | Cross-device Runtime Identity Contract | `cross-device runtime identity gate` | Visible device runtime identity records and catalog hydration state | Exact app/docs SHA parity, catalog/docs revision equality, counts, and bounded freshness status | Branch names are informational only; missing identity, revision mismatch, stale hydration, or retry exhaustion fails closed and blocks parity or release. | Implement the shared Knowgrph runtime identity projection and revision-keyed catalog hydration owner, then capture two-device evidence before protected release. | 2026-07-16 |
| XR motion-reference and canonical Timeline media protected release 2026-07-16 | Promote the validated grey-box stage, cast and camera choreography, Gaussian splat editor, single Camera owner, and canonical Timeline media bars through protected Dev and one production deployment. | Preserve external projects as inspiration only; validate XR staging, Gaussian splat tooling, one Camera owner, and canonical Timeline video/audio bars; integrate through protected Dev; synchronize the generated Prod mirror; deploy the exact merged SHA once; verify both Cloudflare routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `knowgrph/{canvas/src,docs,.github/workflows/release.yml}`; `huijoohwee/content/knowgrph` | XR stage, Gaussian splat surface, shared Camera owner, canonical Timeline media clips, and release ledger | `buildXrMotionReferencePackage`, `serializeOptimizedGaussianPly`, `StrybldrCameraFramingSection`, `VideoSequenceTimelineRuler`, `Production Release` | Attributed Knowgrph task branch, PR #94 runtime-identity handoff, strict planning row, protected Dev SHA, clean generated mirror, and deployment authorization | Deterministic motion-reference package, optimized Gaussian PLY, one Camera surface, canonical thumbnail and waveform bars, exact promoted SHA, and verified production routes | Blockout and SuperSplat remain reference-only with no copied code or external runtime dependency; shared XR, Camera, and Timeline owners stay authoritative; no direct main push, manual mirror edit, deployment before protected integration, or release without exact runtime identity. | Complete independent validation and reconciliation while PR #94 runs; after its handoff, prove two-device SHA parity, merge through the Integration Gate, promote canonically, deploy once, and record immutable route evidence. | 2026-07-16 |
| Probe Tree Widget Card BYOK output protected release 2026-07-16 | Promote slash-only Probe-Tree invocation and visible Widget Card branch output through protected Dev and production gates. | Validate prompt isolation, typed branch materialization, Rich Media publication, output recovery, TypeScript, hygiene, and integration; merge through protected Dev; synchronize the canonical Prod mirror; deploy the merged SHA once; verify both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `knowgrph/{canvas/src,docs,mcp}`; `huijoohwee/content/knowgrph` | Probe-Tree Widget Card release ledger | `runStoryboardWidgetWorkflowProbeTree`, `publishStoryboardWidgetRichMediaOutput`, `useTextWidgetOutputArtifactRecovery`, `Production Release` | Attributed Probe-Tree task branch, BYOK configuration, protected merge SHA, clean generated mirror, and deployment authorization | Slash-only invocation, visible bounded branch cards and Rich Media output, exact promoted SHA, and verified production routes | Shared prompt, graph, Storyboard, publication, and recovery owners remain authoritative; direct main pushes, duplicate token insertion, manual mirror edits, paid proof calls, and deployment before protected integration remain forbidden. | Pass append-only, focused runtime, protected integration, mirror parity, deployment, and live route gates; stop at the first mismatch. | 2026-07-16 |

## 2026-07-17

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Probe Tree balanced layout release continuation 2026-07-17 | Resume the protected release after reconciling the owned Probe-Tree branch with the latest Dev source. | Preserve the attributed Probe-Tree commits; merge the fetched Dev base; validate append-only ledgers, balanced branching, pinning, output publication, TypeScript, hygiene, and runtime identity; promote only the protected merge SHA; synchronize canonically; deploy once; verify both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `knowgrph/{canvas/src,docs,mcp}`; `huijoohwee/content/knowgrph` | Probe-Tree balanced release ledger | `runStoryboardWidgetWorkflowProbeTree`, `resolveStoryboardWidgetProbeTreeBalancedPositions`, `resetStoryboardWidgetWorkflowLayout`, `Production Release` | Reconciled Probe-Tree branch, recorded planning base, protected Dev SHA, clean generated mirror, and production authorization | Balanced left-to-right cascading branches, persistent output, exact promoted SHA, immutable deployment evidence, and verified production routes | Shared graph, Storyboard, layout, pinning, publication, and release owners remain authoritative; concurrent workspace notes stay outside the release snapshot; any ledger, identity, integration, mirror, credential, or route mismatch stops downstream mutation. | Merge this planning row first, complete protected Dev integration, regenerate the Prod mirror, deploy the exact snapshot once, and record route and SHA parity. | 2026-07-17 |
| Repository-owned one-command collaboration gate 2026-07-17 | Replace physical-device and manual runtime-identity export procedures with one deterministic repository command. | Own one Agentic Canvas OS command that delegates to Knowgrph's canonical readiness harness; automatically boot isolated owner/guest browsers and worker, verify active peers, document sync, identity parity, and digest; clean up; forbid physical-device and JSON-export requirements. | `agentic-canvas-os/{package.json,scripts,docs,__tests__,todo/2026-07.md}`; `knowgrph/{scripts,canvas}` | Automated collaboration compliance owner | `collaboration:gate`, `runCollaborationGate`, `collaboration:readiness:check` | Canonical sibling repositories and local loopback ports | One exit code plus machine-readable two-peer runtime proof and failure screenshots | Agentic Canvas OS owns orchestration; Knowgrph owns runtime implementation; no duplicated verifier, physical hardware prerequisite, clipboard transfer, or committed identity artifact is allowed. | Keep the wrapper thin, validate its owner contract, run the complete gate, and integrate through the existing protected runtime-identity PR. | 2026-07-17 |
| XR Camera MCP invocation protected release 2026-07-17 | Promote one always-visible Camera owner with source-backed framing and motion invocation across every surface mode. | Define Camera slash, binding, and semantic tokens upstream; remove the downstream catalog copy; validate shared framing, MCP control, XR choreography, canonical BottomPanel Timeline transport, protected integration, mirror parity, one deployment, and both production routes. | `agentic-canvas-os/{docs/{FACTS.md,DICTIONARY-COMMAND.md,DICTIONARY-SEMANTIC.md,DICTIONARY-BINDING.md},todo/2026-07.md}`; `knowgrph/canvas/src`; `huijoohwee/content/knowgrph` | Shared Camera invocation and release ledger | `/camera.frame`, `/camera.animate`, `/camera.play`, `/camera.scrub`, `control_local_camera`, `Production Release` | Attributed XR task branch, canonical dictionaries, selected actor, stage marks, protected Dev SHA, clean generated mirror, and release authorization | One upstream invocation catalog, always-visible Camera controls, canonical BottomPanel motion track, exact promoted SHA, and verified production routes | Agentic Canvas OS owns token metadata; Knowgrph owns runtime execution; shared Camera and Timeline utilities remain sole state owners; no copied catalog, FloatingPanel timeline, direct main push, manual mirror edit, external runtime dependency, or premature deployment is allowed. | Merge the upstream dictionary row first, validate and merge Knowgrph through the Integration Gate, synchronize the exact Dev SHA canonically, deploy once, and record route and revision evidence. | 2026-07-17 |
